# SaveLink 回归验收报告 - 2026-07-02

## 版本历史

| 版本 | 修改人 | 时间 | 备注 |
| --- | --- | --- | --- |
| 1.0 | Codex | 2026-07-02 | 第一版：记录 SaveLink MVP 重点回归验收结果 |
| 1.1 | 代强 | 2026-07-29 | 补齐版本历史；迁入 doc 目录并改为中文文件名 |
| 1.2 | 代强 | 2026-08-05 | 标注本报告是旧版本历史验收，避免将自动恢复前备份误认为 v0.1.0 行为 |

> 历史口径说明：本报告记录的是 2026-07-02 版本，当时恢复会创建“恢复前自动备份”。该功能已于 2026-07-28 删除；v0.1.0 直接恢复，不自动新增快照，失败安全由同盘替换和回滚保证。

## 结论

本轮按总规划建议做了当前 MVP 的重点回归验收，并补验了新增的 `TC-41 启动自检清理上次中断残留`。

结论：未发现阻塞问题，可以进入下一功能阶段。建议下一阶段优先做“真实恢复进度事件”。

## 测试基线

- 分支：`master`
- 最新提交：`c9e057b fix: run startup self check on app launch`
- 运行方式：当前源码的 Tauri dev 窗口
- 测试数据目录：`C:\Users\daiqiang\door\project_workspace\save_link_workspace\acceptance-data\formal-regression-20260702`
- 用户数据目录：`C:\Users\daiqiang\AppData\Roaming\com.daiq.savelink`

## 自动验证

- `savelink-core`：`cargo test --no-fail-fast` 通过，34 个集成测试全绿。
- `savelink-app`：`npm run build` 通过。
- 说明：`npm run build` 在普通沙箱中曾因父目录读取权限失败，提升权限后通过；这是运行环境限制，不是代码失败。

## 真实窗口回归通过项

- 启动应用：窗口正常打开，标题为 `SaveLink`。
- 添加游戏：填写测试存档目录、测试读取、保存并创建均通过。
- 创建快照：首次快照成功；修改文件后再次创建快照成功。
- 恢复旧快照：恢复成功，并生成 `恢复前自动备份`。
- 替换语义：恢复旧快照后，目标快照没有的 `extra.sav` 被移除，确认不是合并式恢复。
- 恢复 `恢复前自动备份`：可回到恢复前状态。
- 设置页：版本、用户数据目录、仓库目录、数据库路径显示正常；复制路径、打开数据目录、打开仓库目录均正常，无 Tauri opener 权限错误。
- 编辑游戏：修改游戏名和存档路径后保存成功，详情页显示新名称和新路径。
- 缺失目录恢复：
  - 触发后进入 `存档目录不存在` 选择页。
  - 点击 `取消` 后不创建目录、不写入文件。
  - 点击 `创建目录并恢复` 后目录被重建，目标快照内容恢复进去，并显示 `恢复完成`。
- 坏快照恢复：
  - 临时破坏 `.ok` 标记后执行恢复。
  - 失败页显示 `快照已损坏` 和 `本次恢复未修改你的真实存档。`
  - 测完后已恢复被临时破坏的 `.ok` 标记。
- 存档目录不存在时创建快照：
  - UI 提示 `存档目录不存在`。
  - 数据库未新增快照。
  - 磁盘上没有创建该不存在目录。
- 移除游戏：
  - 确认弹窗显示 `不会删除真实存档目录`。
  - 点击 `确认移除` 后，UI 显示 `游戏已移除`，左侧列表不再显示该测试游戏。
  - 数据库中的测试游戏记录和 5 条快照记录均已删除。
  - 对应 5 个仓库快照目录和 `.ok` 文件均已删除。
  - 真实测试存档目录仍存在，4 个文件仍保留。

## TC-41 启动自检

构造方式：

- 在 `savelink.db` 中插入一条 `status = writing` 的测试快照记录。
- 在仓库 `repository\snapshots` 下创建同名半成品快照目录和 `.ok` 标记。
- 重启当前源码 Tauri dev 窗口。

结果：

- 应用正常启动。
- 时间线未显示该 `writing` 残留快照。
- 数据库中的 `writing` 记录被清理。
- 仓库半成品目录和 `.ok` 标记被清理。
- 最终检查 `writing_count = 0`，无 `tc41_dangling_*` 残留。

结论：`TC-41` 通过。

## TC-17 补充验证

用户已明确授权执行 `确认移除测试游戏` 后，补测 `TC-17 移除游戏但不删除真实存档`。

结果：

- 通过真实 Tauri 窗口点击 `编辑游戏` -> `移除游戏` -> `确认移除`。
- UI 显示 `游戏已移除`。
- `formal-regression-20260702-edited` 从左侧游戏列表消失。
- `savelink.db` 中对应游戏记录已删除。
- `savelink.db` 中对应 5 条快照记录已删除。
- 仓库中对应 5 个快照目录和 `.ok` 文件已删除。
- 真实测试存档目录 `acceptance-data/formal-regression-20260702/edit-target` 仍存在。
- 真实测试存档目录内 `config.ini`、`extra.sav`、`slot1.sav`、`slot2.sav` 仍存在。

结论：`TC-17` 通过。

## 测试残留

- 工作区存在未跟踪测试数据目录：`acceptance-data/formal-regression-20260702/`。
- 该目录是本轮验收数据，不建议提交。
- 当前没有发现真实测试存档被错误删除或错误覆盖。

## 建议

当前 MVP 基线可认为稳定。`TC-17` 已补测通过，建议进入下一阶段：真实恢复进度事件，把后端已有的备份当前存档、恢复临时目录、替换真实目录、校验完成等阶段 emit 到前端。
