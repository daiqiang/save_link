# SaveLink 开发进度

## 版本历史

| 版本 | 修改人 | 时间 | 备注 |
| --- | --- | --- | --- |
| 1.0 | 代强 | 2026-06-24 | 补齐版本历史，清理过期路线状态，统一为 MVP 已完成并打包 |
| 1.1 | Codex | 2026-07-01 | 同步 6 月 30 日后续补齐：编辑/移除游戏、缺失目录创建恢复、设置页、打包脚本、Tauri opener 权限修复 |
| 1.2 | Codex | 2026-07-02 | 基线收口：同步当前状态、补齐已知待办、复验 core/build/轻量端到端冒烟；接入 Tauri 启动自检 |
| 1.3 | 代强 | 2026-07-08 | 同步恢复校验增强与云同步增量协议路线 |
| 1.4 | 代强 | 2026-07-14 | 同步百度网盘 POC、协议 v1、云同步基础设施和 Fake 双设备闭环；core 增至 50 个测试 |
| 1.5 | 代强 | 2026-07-15 | 完成 BaiduNetdiskStore、OAuth 本机授权与 Token 持久化、上云按钮授权入口及真实百度验证；core 增至 62 个默认测试 |
| 1.6 | 代强 | 2026-07-16 | 跑通百度真实双设备上传、接收、目录绑定和安全恢复完整闭环；默认测试保持 64 个 |
| 1.7 | 代强 | 2026-07-17 | 确认 v0.1 MVP 功能完成；新增长期自用与对外试用的 6 项发布收口待办 |
| 1.8 | 代强 | 2026-07-23 | 记录真实用户反馈并优化上传入口；将低频百度账号切换移出 v0.1 P0 |
| 1.9 | 代强 | 2026-07-28 | 第一版移除恢复前自动保护点并强化失败回滚；同步绑定/绿色版说明、设置入口和自定义图标状态；默认测试增至 68 个 |
| 1.10 | 代强 | 2026-07-29 | 迁入 doc 目录；同步中文文档名和引用 |
| 1.11 | 代强 | 2026-08-05 | 同步 v0.1.0 发布与 v0.2.0 专项验收；完成自动检查、自动上云、30 条联合清理，并修复跨设备快照时间混存、排序和展示问题 |
| 1.12 | 代强 | 2026-08-06 | 完成时间修复绿色版与真实百度复验；确认 v0.2.0 功能完成，统一版本元数据并记录发布后故障验收项 |
| 1.13 | Codex | 2026-08-10 | 同步 Steam 自动发现、多存档目录、93 个默认测试和真实绿色版验收；修复 Elden Ring 父子路径重叠并补安全校验 |
| 1.14 | Codex | 2026-08-11 | 完成 Elden Ring 修复后真实 Steam 开发版与绿色版回归，并验证重叠目录拒绝 |
| 1.15 | 代强 | 2026-08-12 | 完成 v0.3.0 版本统一、自动验证和正式绿色版构建，进入标签与 Release 收口 |
| 1.16 | 代强 | 2026-08-14 | 完成 v0.4.0 DeSmuME ROM 身份、精确 `.dsv` 快照/恢复、SQLite/云协议兼容和核心/Tauri 流程；修复目录环递归及 1 MiB ROM 哈希栈缓冲区导致的栈溢出，待真实窗口复验 |
| 1.17 | 代强 | 2026-08-15 | 确认 ROM 扫描闪退修复实测通过，统一 v0.4.0 版本元数据并进入正式构建与 Release 收口 |
| 1.18 | 代强 | 2026-08-17 | 确认 v0.4.0 已发布；新增快捷方式、EXE、游戏目录识别和 Manifest 存档候选，默认测试增至 108 个 |
| 1.19 | Codex | 2026-08-18 | 修复无 AppID 程序名称前缀误判；完成真实学习版三种入口及改动文件格式验收，默认测试增至 109 个 |
| 1.20 | Codex | 2026-08-25 | 完成动态发现阶段 1 至 5：待配置与启动绑定、原生监听、候选确认、首次自动快照和真实游戏验收 |
| 1.21 | Codex | 2026-08-26 | 修复 RUNE/CODEX 公共文档存档不在 RDCW 范围的问题，按数字 AppId 归并游戏级候选，RUNE 真实复验通过 |
| 1.22 | Codex | 2026-08-28 | 收紧错误 AppID 的名称佐证，移除游戏目录用户入口，保护浏览器资料目录，增加已配置游戏仅启动，并同步 HowManyDudes 真实验收与 134 项测试基线；快照保留数量改为默认 10、可配置 1 到 100 |
| 1.23 | Codex | 2026-08-29 | 完成快照普通/锁定分区编号、锁定解锁待整理和十分钟维护整理；自动创建关闭时仍继续保留清理与云端收尾 |

> 持久化的路线图与当前位置。上下文压缩不影响此文件——以它为准。
> 最后更新：2026-08-29（快照时间线分区编号与维护整理已实现并通过自动验证）

## 当前位置

**SaveLink v0.4.0 已发布。发布后的游戏存档动态发现主线已完成：未知存档游戏可先登记 EXE，由 SaveLink 启动并监测，用户退出游戏后从候选中确认真实存档目录，随后生成首份自动快照。`Hole Is Mine`、RUNE 版《暗黑地牢》和 `HowManyDudes` 已完成真实绿色版验收。**

当前核心闭环：

```text
Steam 自动发现、游戏程序识别、DeSmuME 扫描或手动添加一个或多个存档来源
  -> 创建快照
  -> 时间线查看
  -> 修改备注 / 锁定 / 删除快照
  -> 恢复指定版本
  -> 缺失目录时创建目录并恢复
  -> 首页查看版本号；设置页控制全局自动备份
```

当前状态条：

```text
做什么(产品文档)           ████████████ 100%  ✅
长什么样(原型图)           ████████████ 100%  ✅ demo-front-1，纯前端 mock
怎么实现(架构文档)         ████████████ 100%  ✅ 已按实际实现同步偏差
验收标准(core 测试)        ████████████ 100%  ✅ 137 项通过，另有 J/L/Q5 三条真实环境测试按需运行
────────────────────────────────────────
第1步 核心逻辑             ████████████ 100%  ✅ A/B/C/D/E 组
第2步 SQLite 持久化        ████████████ 100%  ✅ SqliteRepo 落盘；F 组持久化
第3步 Tauri 外壳+命令层     ████████████ 100%  ✅ 本地 MVP + 百度账号连接命令已接通
第4步 React 前端           ████████████ 100%  ✅ 主界面/弹窗/抽屉；首页版本号和自动备份设置
第5步 前后端接线           ████████████ 100%  ✅ invoke 收口在 api.ts
第6步 Windows 打包          ████████████ 100%  ✅ MSI + NSIS + 绿色 exe
云端可行性 POC          ████████████ 100%  ✅ OAuth/上传/下载/校验/基准测试
云端快照协议 v1         ████████████ 100%  ✅ 目录/格式/发现/校验/失败规则已定稿
云同步本机基础设施       ████████████ 100%  ✅ SQLite/CloudStateRepository/FakeCloudObjectStore
Fake 双设备同步闭环      ████████████ 100%  ✅ 协议 JSON/zip/上传/发现/下载/接收落地
百度正式适配器         ████████████ 100%  ✅ 本地 HTTP 契约 + 真实对象存取冒烟
百度账号首次连接        ████████████ 100%  ✅ 浏览器授权/本机回调/Token 落盘已实测
真实单快照上云          ████████████ 100%  ✅ zip/.ok + 状态 UI 已实测
真实百度发现/下载/接收   ████████████ 100%  ✅ 设备 B 隔离数据目录实测通过
MVP 后补齐                 ████████████ 100%  ✅ 编辑/移除/首页版本号/缺失目录恢复
v0.1.0 公开发布             ████████████ 100%  ✅ GitHub Release/Tag/绿色版 ZIP/SHA-256
v0.2.0 自动备份与上云         ████████████ 100%  ✅ 主流程、旧库迁移及真实百度跨设备验收通过
Steam 自动发现              ████████████ 100%  ✅ 主链路及 Elden Ring 父子路径回归通过
多存档目录                  ████████████ 100%  ✅ 快照/恢复/云协议/UI/旧库迁移及真实窗口验收通过
v0.3.0 发布收口             ████████████ 100%  ✅ GitHub Release/Tag 已发布
v0.4.0 DeSmuME 支持        ████████████ 100%  ✅ GitHub Release/Tag 已发布
游戏程序/快捷方式识别       ████████████ 100%  ✅ 快捷方式/EXE 实机验收通过，目录扫描保留后端回归
动态发现阶段 1-5            ████████████ 100%  ✅ 监听、候选确认、首份快照及真实游戏验收完成
```

## 当前主线：游戏启动与存档动态发现

权威方案见 `SaveLink游戏启动与存档动态发现方案.md`。截至 2026-08-28：

- 阶段 1 Windows POC 已完成：原生 `ReadDirectoryChangesW` 多目录监听、主动取消、长路径、PID/宽限期和溢出语义均已验证。
- 阶段 2 已完成：`games.launch_binding` SQLite 迁移、待发现/待绑定状态、空来源安全限制、普通 EXE/Steam 启动绑定和 DeSmuME 原绑定兼容均已接通。
- 普通 PC 游戏新建时必须保存本机启动方式；Steam 绑定固定为 `steam.exe + -applaunch AppID`，不接受任意命令行。
- 新增 `add_program_game`、`register_steam_game`、`bind_program_to_game`；EXE/安装目录/Steam AppID/存档目录跨入口防重，旧游戏可补绑而不新增重复记录。
- 本机正式 SQLite 已在备份后事务修复：奥术扳机绑定 `GunWizard.exe`，艾尔登法环绑定 AppID `1245620`，杀戮尖塔 2 绑定 AppID `2868840`；无快照重复记录已删除，25 个快照无孤儿，数据库完整性为 `ok`。
- 没有真实 EXE 的历史验收测试数据没有伪造绑定，保留为旧待绑定数据。
- 隔离窗口已验证 Steam 绑定持久化和重复扫描“已添加”；普通旧游戏选择同安装目录 EXE 会绑定原记录而不重复创建。
- 阶段 3 已完成：原生 `ReadDirectoryChangesW` 多根监听、全局单会话、受控启动、PID 生命周期、5 秒宽限期、手动停止和有界候选分析已接入正式应用。
- 阶段 4 已完成：候选默认不选择，确认时由后端重新校验成员、安全边界、重复/嵌套及跨游戏冲突；确认后保存来源，并在自动备份开启时创建当前游戏的首份自动快照。
- 阶段 5 已完成：针对目录通知遗漏文件事件增加游戏身份相关、时间窗口受限的会话结束补查；噪声候选保持低可信。`Hole Is Mine` 的真实 `Release` 目录成为唯一高可信候选，确认后快照 6 个文件、39,670 字节与源目录哈希逐一一致。
- 《暗黑地牢》RUNE 学习版确认真实存档位于公共文档 `Steam\RUNE\262060`，且 `filemappings.ini` 在 `remote` 之外也随游戏进度变化。当前代码会通过 Known Folder API 条件性加入已存在的 `Steam\RUNE`/`Steam\CODEX` 根目录；如果紧跟纯数字 AppId，则将其下所有变化归并为整个 `<AppId>` 高可信候选。RUNE/CODEX 和未知根目录的自动边界回归已通过；用户真实游玩《暗黑地牢》并退出后，只观察到唯一推荐 `C:\Users\Public\Documents\Steam\RUNE\262060`，RUNE 真实复验通过（未留截图）。
- `HowManyDudes.exe` 携带的 AppID 会错误指向 Manifest 中的 `DrainSim`。当前代码要求 AppID 对应的 Manifest 名称与 EXE、快捷方式或目录身份至少有一项归一化后完整相等；名称冲突时忽略该 AppID 和对应规则，按真实程序身份进入动态发现。
- `HowManyDudes` 真实游玩后，主存档 `C:\Users\daiqiang\AppData\Local\HowManyDudes\<用户编号>\Game` 成为高可信候选；浏览器资料目录被强制降为低可信且禁止确认，`.bak` 配对只作辅助加分，不能单独证明候选是游戏存档。
- 已配置且有普通 EXE/Steam 启动绑定的游戏可通过“启动游戏”直接启动；该动作不创建动态发现监听、不记录 PID、不产生候选，自动备份仍由原有启动检查和 10 分钟轮询负责。

完整设计和验收结论见 `SaveLink游戏启动与存档动态发现方案.md`；本机详细验收记录保留在未入库的 `acceptance-data/save-discovery-stage5-20260825/`。

## 发布后主线：游戏程序与快捷方式识别（已验收）

- 添加游戏弹窗保留 Steam、DeSmuME 和手动添加，并新增“游戏程序”入口。
- 当前“游戏程序”页面只允许选择 Windows `.lnk` 或 EXE；快捷方式通过 Windows Shell COM 只读解析，不启动目标程序。后端保留有限安装目录扫描能力，供内部回归和未来入口复用。
- 优先读取 `steam_appid.txt` 及常见 AppID 配置，但 AppID 对应的 Manifest 游戏名必须与快捷方式、EXE 或目录身份至少有一项完整相等；名称冲突时忽略 AppID。无可靠 AppID 时同样只接受完整名称相等，不接受前缀猜测。
- 复用随包 Ludusavi Manifest 的 Windows/Steam 路径规则，只展示当前真实存在的存档目录，配置路径继续单列且默认不保护。
- 扫描限制在所选游戏目录的 3 层、最多 512 个目录，不遍历整块磁盘，不修改程序、配置或存档。
- AppID、AppID 名称冲突回退、精确名称兜底、目录配置、未知程序拒绝猜测、名称前缀拒绝和真实 Windows `.lnk` 往返均有 N 组测试；真实学习版样本已完成人工只读验收。

## v0.4.0 DeSmuME 模拟器支持（已发布）

v0.4.0 首批只支持 DeSmuME 0.9.x，Yuzu 延后到 v0.5.0。当前实现已完成以下主链路：

- 扫描 DeSmuME 目录、`desmume.ini` 和 ROM 目录，以迭代遍历发现 `.nds` 并跳过目录链接。
- 分块计算 ROM SHA-256，并读取 NDS Header Title 和 Game Code；不上传 ROM。
- 只保护当前 ROM 对应的 `.dsv` 游戏内存档，不把共享 `Battery` 目录或 `.dsv-01` 至 `.dsv-09` 即时存档纳入 v0.4.0。
- 快照内部使用稳定的 `save.dsv` 逻辑文件名；跨设备恢复时映射到目标设备当前 ROM 文件名对应的 `.dsv`。
- 精确 SHA-256 自动匹配；SHA 不同但 Header Title 与 Game Code 同时相同，只作为候选并要求用户确认。
- SQLite、云端 `game.json` 和设备 B 接收流程保存 ROM 身份，但不传播 DeSmuME 根目录、ROM 路径或 Battery 路径。
- 普通游戏继续沿用整目录来源，旧数据库自动兼容迁移。

自动化测试、真实 ROM 目录扫描和栈溢出修复复验均已完成，v0.4.0 已正式发布。

## v0.2.0 自动备份（已完成，已发布）

项目已决定跳过 v0.1.1，直接进入 v0.2.0。当前权威产品方案见 `SaveLink-v0.2.0自动备份方案.md`。

已确认：

- SaveLink 启动后立即检查一次，之后默认每 10 分钟检查一次。
- 自动备份使用全局开关并默认开启，作为设置页第一个正式设置项，不为每个游戏单独配置。
- 关闭自动备份只停止变化扫描和自动快照创建；十分钟维护周期仍继续整理快照区域、清理超额记录和续做云端删除。
- 当前存档与最近成功快照不同才创建，不变则跳过。
- 游戏运行期间允许读取；正式快照必须通过稳定性和完整性校验。
- 本地快照创建成功后立即上传百度网盘。
- 每个游戏默认保留最近 10 个未锁定历史记录，设置页可配置为 1 到 100 个；锁定记录不计入上限且不设数量上限。
- 清理旧记录的最终目标同时覆盖本地仓库和百度网盘；只完成本地清理不算 v0.2.0 保留策略完成。
- 多设备出现冲突时暂停自动覆盖，由用户选择本机、云端或两边都保留。

当前实现已统一计数手动、自动和云端接收的未锁定快照。新安装默认使用 10 条；已有游戏的旧数据库会先保留超出快照，用户在设置页确认后才按新上限清理。降低上限会触发现有云端/本地联合清理；云端删除失败时保留本地文件和记录并写入 `delete_failed`，后续周期重试。普通区和锁定区分别动态编号，锁定/解锁在维护周期前保留原区域并显示待整理状态。

跨设备快照时间已统一：SQLite 中的快照时间固定保存为秒精度 UTC RFC 3339，旧版本机分钟格式和带偏移 RFC 3339 会在打开数据库时自动迁移；前端统一转换为用户本地时间显示。混合来源快照的最近记录、内容去重和保留数量淘汰均按真实时刻判断，已有云端协议 v1 对象无需重传。

### 发布后补充验收（不阻塞 v0.2.0）

- 真实断网时自动上传失败、保留本地和恢复联网后的重试。
- Token 失效及百度限流时的自动上传恢复。
- 真实百度云端删除中途失败后，`delete_failed` 的后续续做。
- 云对象已被另一台设备提前删除时的真实百度幂等清理。
- 多设备同时产生不同进度时的冲突识别与用户选择交互。

## v0.1.0 已发布与发布后收口（并行待办，不阻塞 v0.2.0）

以下清单记录 v0.1.0 发布后仍需完成的事项。项目已明确不单独发布 v0.1.1，这些事项与 v0.2.0 主线并行，由后续优先级决定何时处理。

- [ ] **真实双电脑长期自用**：2026-07-23 已由代强和朋友在两台真实电脑完成首次全链路验收；继续在实际游戏、不同网络和跨设备日常使用中观察并记录问题。
- [x] **普通用户百度授权**：2026-07-23 已使用非开发者普通百度账号完成授权、上传、发现、下载、绑定和恢复。普通用户只需授权 SaveLink，不需要注册开发者、创建百度应用或预先启动百度网盘客户端。
- [ ] **故障与重试回归**：覆盖上传断网、下载断网、授权过期/失效、下载中断和重试；确认失败不会出现完整快照假象、不会留下不可恢复半成品，也不会修改真实游戏存档。
- [ ] **发布体验收口**：SaveLink 云链 V9 自定义图标已替换，窗口/任务栏显示已实机确认；剩余一致的版本信息、正式包工程测试标识清理、托盘/安装包图标回归，以及确保测试脚本和测试数据不进入普通用户使用流程。
- [ ] **凭据安全与平台准入**：v0.1.0 仍使用 AppData 明文 Token 文件；优先迁移到 Windows DPAPI、Credential Manager 或 Tauri Stronghold，并继续确认百度应用审核、权限申请、材料和发布条件。
- [x] **发布 v0.1.0**：已创建 `v0.1.0` 标签和 GitHub Release，上传绿色版 ZIP 与 SHA-256；GitHub 为主仓库，Gitee 为国内镜像，代码和标签已同步。
- [ ] **发布产物与标签对齐**：当前本地绿色版 ZIP 生成于 2026-07-30 11:09，而 `v0.1.0` 包含 11:27 的“托盘增加划线”提交。需从 `v0.1.0` 对应提交重新打包、复测，并替换 GitHub Release 附件。
- [ ] **更新公开页面截图**：原 README 截图仍展示已删除的“恢复前自动备份”行为，已暂时从 README 隐藏；需用 v0.1.0 当前界面重新截图后恢复展示。

长期自用、故障回归、凭据安全和发布产物修正可以并行；任何影响真实存档写入的修复仍必须先通过 core 测试和人工假目录验收。

## 已完成能力

### 游戏管理

- 添加游戏，选择单个存档目录。
- 测试读取目录，显示文件数量和总大小。
- 游戏列表展示名称、快照数量、最近快照时间。
- 编辑游戏名称与存档路径。
- 编辑时可重新选择目录、测试读取、保存修改。
- 移除游戏：删除 SaveLink 内部游戏记录、该游戏快照记录和快照仓库文件。
- 移除游戏不会删除真实存档目录。

### 快照管理

- 手动创建快照。
- 存档未变化时返回 NoChange，不重复创建。
- 时间线展示创建时间、备注、文件数量、大小、创建原因。
- 快照详情抽屉可修改备注；备注目前承担“快照命名”的作用。
- 无备注快照按显示区域和创建时间动态显示为“存档N”或“锁定存档N”，锁定区始终置顶；手动备注优先，不被自动编号覆盖。
- 快照可锁定/取消锁定。
- 锁定/解锁立即改变清理保护状态，但原区域和编号保留到下一次十分钟维护；界面显示“已锁定，待整理”或“已解锁，待整理”。
- 锁定快照不可删除。
- 未锁定快照可删除。
- 旧版 `before_restore` 记录作为普通历史备份兼容展示，当前版本不再创建。

### 恢复流程

- 恢复前先校验目标快照；目标损坏则不碰真实存档。
- 当前已等于目标时不覆盖；其余情况直接恢复，不自动创建或复用恢复前快照。
- 确认弹窗明确提醒第一版不会自动备份当前存档，用户可先手动创建快照。
- 覆盖恢复采用同盘临时目录 + rename 替换；`.old` 保留到最终校验通过后才删除，最终校验失败会换回原目录。
- 恢复后校验真实存档内容与目标快照一致。
- 恢复失败页已按错误类型精修：
  - 快照损坏、已回滚的恢复失败：明确提示本次恢复未修改真实存档。
  - 极端的未回滚失败：提示真实存档可能已被改动，要求核对存档目录。
- 存档目录缺失时，UI 已支持“创建目录并恢复”和“取消”。
- “重新选择目录并恢复”暂未做；它依赖更完整的编辑路径/恢复联动，暂不算核心缺口。

### 版本信息与设置入口

- 当前版本号直接显示在首页 SaveLink 标题旁，不需要进入设置页查看。
- 顶部齿轮打开设置页，当前第一个正式设置项是“自动备份”全局开关，默认开启并显示 10 分钟检查间隔。
- 设置状态保存在 SQLite `app_settings.auto_backup_enabled`；从关闭切换为开启时立即执行一次检查。
- `get_app_info` 以及打开/复制 AppData 路径的底层实现仍保留，当前设置页不展示这些内部路径。
- 绿色版和安装版继续共用 `%APPDATA%\com.daiq.savelink` 数据目录。
- 保留的 `@tauri-apps/plugin-opener` 权限只允许 `$APPDATA` 与 `$APPDATA/**`，不开放整个磁盘。

### 打包

- `savelink-app/build-portable.bat` 用于生成面向普通用户发布的绿色版目录、ZIP 和 SHA-256 文件。
- `savelink-app/build-installer.bat` 用于生成绿色 exe、NSIS 和 MSI。
- 两个脚本都会自动定位 MSVC 环境并在构建前关闭本工作区正在运行的 SaveLink。
- 脚本已具备针对锁文件/瞬时错误的重试能力。

### 百度账号连接

- 快照时间线已提供“上云”按钮；未连接时触发百度官方 OAuth 页面。
- 使用系统默认浏览器授权，并由 `127.0.0.1:53682/oauth/callback` 本机回调接收授权码。
- 回调校验随机 `state`，同一时间只允许一个授权流程，超时后自动结束等待。
- Token 保存在 `%APPDATA%\com.daiq.savelink\credentials\baidu-oauth.json`；SQLite `cloud_accounts` 只保存 `token_ref`，Token 不传入 React、不写日志。
- 2026-07-15 已用正式绿色版完成真实授权、回调和本机持久化验证。
- 未连接时授权成功后自动继续上传；已连接时直接打包并上传当前快照。
- 快照上传入口已由纯图标改为图标加文字：默认 `上传`、进行中 `上传中`、成功 `已上云`、失败 `重试`；成功状态持久化为 `uploaded`，重复上传由协议层保证幂等。
- access token 临近过期时自动使用 refresh token 刷新并落盘；账号解绑/切换作为低频增强后置，凭据加密是 v0.1.x 的优先安全加固项。
- 2026-07-16 已实测百度应用目录中生成同一快照 ID 的 `.zip` 与 `.ok`。
- 顶栏“云端存档”可按游戏展示云端快照；只读发现阶段不会创建本机游戏。
- 用户点击下载后先校验 zip 大小与 SHA-256，再校验解压后的内容指纹；全部通过后才写入本机 `repository` 和 SQLite。
- 接收成功后才创建未绑定的本机游戏，且绝不带入设备 A 的真实存档路径；绑定前禁止创建快照和恢复。
- 已通过 `%APPDATA%\com.daiq.savelink-device-b-test` 隔离数据目录实测，顶栏明确显示“设备 B 隔离测试”，不会触碰正式 SaveLink 数据目录。
- 未绑定游戏的主操作为“绑定存档目录”：选择路径后先只读扫描，扫描成功才允许确认；绑定只更新本机 SQLite 路径，不创建快照、不恢复、不上传。
- 目录为空时明确说明绑定后仍需用户另行选择快照恢复；目录已有文件时，绑定页和恢复确认页都提醒用户恢复不会自动备份，可先手动创建快照。
- 2026-07-16 设备 B 已绑定假目录 `D:\door\模拟存档\艾尔登法环-机器2` 并手动恢复，目标快照恢复出 1 个文件、54 B，恢复文件与本机快照仓库副本 SHA-256 完全一致。旧版曾生成恢复前备份，当前版本不再创建。

## 安装包产物

```text
savelink-app/src-tauri/target/release/
├── savelink-app.exe                         绿色版，可免安装直接运行
└── bundle/
    ├── portable/SaveLink_0.1.0_windows_x64_portable.zip
    ├── portable/SaveLink_0.1.0_windows_x64_portable.zip.sha256.txt
    ├── nsis/SaveLink_0.1.0_x64-setup.exe    NSIS 安装程序，推荐给普通用户
    └── msi/SaveLink_0.1.0_x64_en-US.msi     MSI 安装包
```

运行时数据位置：

```text
%APPDATA%\com.daiq.savelink\
├── credentials\baidu-oauth.json
├── savelink.db
└── repository\
```

绿色版和安装版使用同一个 Tauri identifier：`com.daiq.savelink`，因此共享同一个用户数据目录。

## 当前测试状态

`savelink-core`：

```text
cargo test --no-fail-fast
```

当前结果：**134 项通过，J/L/Q5 三个真实环境测试默认忽略**；J/L 已按需执行通过，Q5 真实 DeSmuME 只读发现测试已执行通过；Tauri 32 个测试全绿。

分组：

- A 组：创建快照，8 个。
- B 组：恢复，14 个。
- C 组：删除/锁定/删除游戏，6 个。
- D 组：存储/扫描，6 个。
- E 组：启动自检/同盘检测，3 个。
- F 组：SQLite 持久化及旧快照时间迁移，4 个。
- G 组：云同步本机基础设施和旧库状态迁移，8 个。
- H 组：Fake 双设备云同步闭环、联合删除失败重试、旧云端时间幂等兼容、模拟器身份升级及启动绑定云端隔离，17 个。
- core 内部单元测试：百度路径映射/错误分类、时间规范化和多目录恢复回滚，4 个。
- `save_activity.rs` 内部单元测试：事件聚合、候选评分、RUNE/CODEX 归并、浏览器目录保护和 `.bak` 辅助信号，共 16 个；core 当前内部单元测试合计 20 个。
- I 组：百度 HTTP 契约，4 个。
- J 组：真实百度对象存取冒烟，1 个，默认忽略；2026-07-15 已执行通过。
- K 组：百度 OAuth URL、换/刷新 Token、Token 文件仓库、本机回调和 `state` 校验，8 个。
- L 组：真实百度只读发现与设备 B 接收落地，1 个，默认忽略；2026-07-16 已执行通过。
- M 组：自动检查、单游戏失败隔离、可配置保留候选及混合时间排序回归，5 个。
- N 组：Steam 多游戏库发现、路径规则和程序识别，含 AppID、AppID 名称冲突回退、精确名称、快捷方式及保守拒绝，共 14 个。
- O 组：多目录指纹、`FsStore` 根目录边界、整体恢复、旧 SQLite 单目录迁移、重叠来源和 Windows verbatim 路径，6 个。
- P 组：共享 `Battery` 目录中的精确 `.dsv` 快照、恢复和其他游戏文件隔离，2 个。
- Q 组：DeSmuME 目录/ROM 发现、NDS Header 身份、SHA-256 缓存、`.dsv` 精确匹配、即时存档排除、目录链接环防护和小栈线程哈希回归，5 个默认测试；另有 1 个真实环境测试默认忽略。
- 待配置游戏 Q 组：状态推导、SQLite 迁移/重开、空来源操作拒绝和移除安全，4 个。

前端/打包已验证：

- `npm run build` 通过。
- v0.3.0 的 npm、Cargo、Tauri 和打包版本元数据已统一为 `0.3.0`；正式绿色版已通过独立 Cargo target 构建并完成内容与 SHA-256 核验，GitHub Release 已发布。
- v0.3.0 绿色版位于 `savelink-app/src-tauri/target-v030/release/bundle/portable/`，ZIP SHA-256 为 `0BA1B1309C778D7DA2EED0D5C033807E0D0E8CA9CB3EFD3380A1DDECFFB4FD70`。本机沙箱无法覆盖旧 `target/release` DLL，因此使用独立 target；不影响二进制内容和发布。
- `build-installer.bat` 打包通过。
- 绿色版实机打开验证通过。
- `v0.1.0` GitHub Release 已发布绿色版 ZIP 与 SHA-256；当前附件需按发布标签重新打包一次，以纳入最后的托盘菜单提交。
- 旧设置对话框的“打开”数据目录/仓库目录代码路径曾复测通过；当前齿轮入口展示自动备份开关，本轮视觉尚待绿色版验收。
- 设备 B 隔离模式实测通过：发现云端快照、下载并校验、接收落地、未绑定目录保护和“已在本机”状态均符合预期。
- 2026-08-06 时间修复绿色版复验通过：30 条旧时间无损迁移、150 个仓库文件哈希零变化、二次启动幂等；真实百度设备 B 本地化显示、两阶段去重和混合来源联合删除均通过。
- 2026-07-23 两台真实电脑、普通百度账号按 `手写文档/测试清单.md` 完成全链路验收；测试期间无需在任一电脑预先打开百度网盘客户端。
- 2026-08-10 绿色版使用本机真实 Steam 完成 8 个候选发现；《杀戮尖塔》4 个存档目录添加、编辑到隔离假目录、A/B 快照往返恢复、详情展示和移除安全均通过。当前机器只有 1 个 Steam 游戏库，多库仅由 N 组自动测试覆盖。
- 2026-08-10 修复 Elden Ring 的 `<storeUserId>` 规则误匹配同级 `GraphicsConfig.xml`：末尾用户目录占位符只接受目录，Manifest 结果收敛父子路径；添加、编辑、创建和恢复入口统一拒绝重叠目录。
- 2026-08-11 使用真实 Steam 完成修复后开发版与绿色版定向回归：Elden Ring 只保护数字用户目录，配置父目录只进入 `config_paths`；《杀戮尖塔》4 个目录无回归，相同及父子嵌套目录均被拒绝。
- 2026-08-18 使用真实学习版 `Arcane.Trigger.Build.21206983` 完成绿色版定向回归：首次发现其名称被错误按前缀命中 `Arcane / 534870`；修复后快捷方式、`GunWizard.exe` 和安装目录均返回 0 个匹配，程序未启动，既有手动记录和真实存档未修改。
- 2026-08-25 动态发现阶段 2 回归：core 116 项、Tauri 10 项和前端构建全绿；隔离窗口验证 Steam 启动绑定持久化、AppID 防重和旧普通游戏 EXE 补绑。正式数据库修复前已备份，修复后完整性为 `ok`，未修改真实存档或快照文件。
- 2026-08-25 动态发现阶段 3 至 5 收口：core 125 项通过（3 项真实环境测试忽略）、Tauri 29 项和前端构建全绿；严格 Clippy 通过。真实 `Hole Is Mine` 监测、候选确认和首份自动快照闭环通过，未恢复、删除或覆盖真实存档。
- 2026-08-25 最终 NSIS、MSI 和绿色版均由仓库脚本构建成功；隔离 profile 启动、关闭到托盘、单实例恢复窗口和退出清理通过。绿色版 ZIP SHA-256 为 `03DF5D1F451F0F36D4CDF3713DEEC5EBD57D00E2311DB16B4B01C664E4109D68`。
- 2026-08-28 当前基线验证：core 134 项、Tauri 32 项、前端构建和严格 Clippy 全绿。真实 `HowManyDudes` 样本验证错误 AppID 不再误命中 `DrainSim`，浏览器资料目录保持不可确认；主存档目录进入高可信候选。新增仅启动回归确认已配置游戏启动时不创建发现会话。

## 已知未完成 / 暂不做

- 本地快照仍由 `FsStore` 以目录形式保存在 `repository` 中。这不阻塞云同步：上云时将单条快照临时打包为 zip。
- 真实多 Steam 游戏库机器尚未验收；代码与 N 组自动测试已覆盖多库枚举和分组。
- 恢复进度事件未真正接到前端；当前恢复页给整体进行中提示，不伪造真实步骤完成态。
- 启动自检 `startup_self_check` 已接入 Tauri 启动路径，可清理 `writing` 半成品并续做 `deleting` 本地删除；真实窗口残留清理场景可在正式回归时补验收。
- 设置入口已提供全局自动备份开关；帮助入口仍为占位 toast。
- 百度 Token 当前为 AppData 用户目录下的独立明文 JSON；v0.2.0 继续沿用该状态，迁移到 Windows DPAPI、Credential Manager 或 Tauri Stronghold 作为后续安全加固项。
- OAuth 已接入过期前自动刷新；尚未提供主动解绑/切换入口，该低频能力不阻塞当前版本，凭据加密优先级更高。
- v0.3.0 和 v0.4.0 均已完成 GitHub Release/Tag 发布；后续开发包不能复用旧发布产物。
- README 的旧页面截图已暂时隐藏，等待使用 v0.1.0 当前界面重新截图。
- 家中验收环境已安装 `rustfmt 1.9.0-stable`；本轮修改的 `steam_discovery.rs` 和 `n_steam_discovery.rs` 已通过 `rustfmt --check`。全仓 `cargo fmt --all -- --check` 可以运行，但会报告既有文件的历史格式差异，因此未在本轮混入全仓机械格式化。
- v0.4.0 真实 ROM 目录扫描及栈溢出修复已通过用户复验并发布。
- 动态发现只把与本次游戏身份和时间窗口相关的目录活动作为推荐依据，不能从 Windows 文件系统事件证明具体写入进程；因此候选始终需要用户确认。
- 某些游戏会同时修改主存档和自带备份目录。当前 `HowManyDudes` 的主存档与 `Backup` 镜像目录都可能进入高可信候选，SaveLink 尚不能通用判定哪一个应被保护；用户应核对真实路径，只选择主存档。

## 后续可做（需总规划会话排优先级）

这些都是 MVP 之后的增强，不是当前阻塞：

- 使用更多不同引擎和不同存档布局的真实学习版游戏扩大动态发现兼容样本，仍坚持候选默认不选择、不自动绑定和不对真实存档执行恢复。
- 得到 CODEX 真实样本后，复验其标准 `Steam\CODEX\<AppId>` 布局；当前仅完成代码和自动测试覆盖。
- 优化主存档与游戏自带 `Backup` 镜像目录并列高可信的提示或排序，但不能用一条通用“父目录优先/子目录优先”规则武断删除候选。
- 更正式的一轮回归验收报告。
- 接入真实恢复进度事件。
- 启动自检真实窗口残留清理场景验收。
- 真 zip / restic 快照存储，降低空间占用与文件数量。
- v0.2.0 自动备份已经完成，具体规则与发布后补充验收见 `doc/SaveLink-v0.2.0自动备份方案.md`。
- 补一台真实多 Steam 游戏库机器验收；当前 N 组自动测试已覆盖多库枚举和分组。
- 低频账号管理：显示真实百度账号身份，支持安全解绑和切换，并按账号隔离本机云状态。
- 云端：协议、真实上传、设备 B 发现/下载/接收、独立绑定及安全恢复均已完成实机验收；下载、绑定和恢复保持三个独立动作。
- 手动 zip 导出/导入：作为备份迁移工具，低优先级；不作为自动云同步主线。
- 安装体验、版本信息一致性和正式包工程测试标识继续收口；自定义图标资产及配置已经完成，托盘/安装包显示纳入 v0.1.x 回归。
- Yuzu 支持仍延后到 v0.5.0。

## 关键事实（防遗忘）

- 工作目录：`C:\Users\daiqiang\door\project_workspace\save_link_workspace`
- 核心 crate：`savelink-core/`
- 桌面应用：`savelink-app/`
- 高保真 mock 原型：`demo-front-1/index.html`
- GitHub 主仓库：`https://github.com/daiqiang/save_link`
- Gitee 国内镜像：`https://gitee.com/qigugu/save-link`
- v0.1.0 Release：`https://github.com/daiqiang/save_link/releases/tag/v0.1.0`
- v0.3.0 Release（发布后）：`https://github.com/daiqiang/save_link/releases/tag/v0.3.0`
- 当前正式状态总结：`doc/SaveLink当前功能完成情况.md`
- v0.2.0 自动备份方案：`doc/SaveLink-v0.2.0自动备份方案.md`
- 交接文档：`doc/HANDOFF-codex.md`
- 云端快照协议 v1：`doc/SaveLink云端快照协议V1.md`
- 百度网盘 POC 正式报告：`doc/百度网盘API-POC报告-20260714.md`
- 百度网盘 POC 外部实验工程（不属于主仓库）：`D:\door\codex_workspace\save_link_poc\baidu-netdisk-poc\`
- 手动测试计划：`savelink-app/手动测试计划.md`
