# SaveLink 开发交接（给 Codex / 后续开发者）

## 版本历史

| 版本 | 修改人 | 时间 | 备注 |
| --- | --- | --- | --- |
| 1.0 | 代强 | 2026-07-02 | 更新当前状态与待办，记录总规划会话注意点；同步基线收口要求；接入启动自检 |
| 1.1 | 代强 | 2026-07-08 | 同步恢复校验增强、35 个 core 测试与云同步增量协议路线 |
| 1.2 | 代强 | 2026-07-14 | 同步百度网盘 POC、协议 v1、本机云基础设施和 Fake 双设备闭环；core 增至 50 个测试 |
| 1.3 | 代强 | 2026-07-15 | 同步 BaiduNetdiskStore、OAuth 本机授权与 Token 持久化、上云入口和 62 个默认测试；明确真实上传下一步 |
| 1.4 | 代强 | 2026-07-16 | 同步百度真实双设备上传、接收、目录绑定和安全恢复完整闭环 |
| 1.5 | 代强 | 2026-07-17 | 将下一阶段切换为 v0.1 长期自用与对外试用发布收口，并指向权威待办清单 |
| 1.6 | 代强 | 2026-07-23 | 同步真实用户反馈与上传入口优化；将低频百度账号切换移出 P0 |
| 1.7 | 代强 | 2026-07-28 | 第一版移除恢复前自动保护点并强化失败回滚；同步绿色版说明、设置入口、自定义图标状态和 68 个默认测试 |
| 1.8 | 代强 | 2026-07-29 | 迁入 doc 目录；同步中文文档名和引用 |
| 1.9 | 代强 | 2026-08-05 | 同步 v0.1.0 发布与 v0.2.0 专项验收；完成自动检查、自动上云、30 条联合清理，并修复跨设备快照时间混存、排序和展示问题 |
| 1.10 | 代强 | 2026-08-06 | 完成时间修复与真实百度复验；确认 v0.2.0 功能完成，统一版本元数据并记录发布后故障验收项 |
| 1.11 | Codex | 2026-08-10 | 同步 Steam 自动发现、多存档目录、93 个默认测试和真实绿色版验收；修复 Elden Ring 父子路径重叠并补入口校验 |
| 1.12 | Codex | 2026-08-11 | 完成 Elden Ring 修复后真实 Steam 开发版与绿色版候选复验，并验证重叠目录拒绝 |
| 1.13 | 代强 | 2026-08-12 | 完成 v0.3.0 版本统一、自动验证和正式绿色版构建，进入标签与 Release 收口 |
| 1.14 | 代强 | 2026-08-14 | 同步 v0.4.0 DeSmuME 实现；修复目录环递归及 1 MiB ROM 哈希栈缓冲区导致的栈溢出，102 个 core 默认测试全绿，待真实窗口复验 |
| 1.15 | 代强 | 2026-08-15 | 确认 ROM 扫描闪退修复实测通过，统一 v0.4.0 版本元数据并进入正式构建与 Release 收口 |
| 1.16 | 代强 | 2026-08-17 | 确认 v0.4.0 已发布；接入快捷方式、EXE、游戏目录识别和 Manifest 存档候选，默认测试增至 108 个 |
| 1.17 | Codex | 2026-08-18 | 修复无 AppID 程序名称前缀误判；真实学习版快捷方式、EXE 和目录只读验收通过，默认测试增至 109 个 |
| 1.18 | Codex | 2026-08-25 | 完成动态发现阶段 1 至 5：启动绑定、原生监听、候选确认、首次自动快照和真实游戏验收 |
| 1.19 | Codex | 2026-08-26 | 修复 RUNE/CODEX 公共文档监测缺口，按数字 AppId 归并为游戏级保护目录，RUNE 真实复验通过 |
| 1.20 | Codex | 2026-08-28 | 同步错误 AppID 防误识别、浏览器资料目录保护、游戏程序入口收紧、已配置游戏仅启动、HowManyDudes 真实验收和 134 项 core 基线；新增默认 10、范围 1 到 100 的可配置快照保留数量 |
| 1.21 | Codex | 2026-08-29 | 同步快照分区维护；完成名称/锁定状态独立云元数据同步、离线重试、旧库迁移和清理保护；基线增至 core 141 项、Tauri 37 项 |
| 1.22 | Codex | 2026-08-30 | 实现快照详情与当前本地存档的只读一致性检查；修复云同步写锁阻塞并增加超时兜底，core 增至 147 项 |
| 1.23 | Codex | 2026-08-31 | 修正不可见后台云同步导致的误导性占用提示；上传开始提示改由后端受理事件触发，Tauri 增至 40 项；云任务排队延至下个版本 |
| 1.24 | Codex | 2026-08-31 | 准备发布 v0.5.0，统一当前版本元数据、README、打包说明和测试产物路径 |
| 1.25 | Codex | 2026-09-01 | 修复云端存档重复冲突、全量元数据下载和失败假空；接入增量缓存、8 路发现及真实百度性能回归，core 增至 150 项 |
| 1.26 | Codex | 2026-09-02 | 完成 v0.6.0 云任务调度与冲突治理设计，确定队列、资源键、可靠删除、metadata revision 和未读提醒实施顺序 |
| 1.27 | Codex | 2026-09-03 | 吸收两轮评审：完善任务取代、tombstone 全入口竞态、上传恢复、设备 ID 迁移、ignored 兼容和规模验收 |

> 角色约定：总规划会话负责方向、范围和验收判断；本地开发会话负责按文档实现、验证、打包。
> 开工前先读完本文档 + `PROGRESS.md`，再动代码。
> 最后更新：2026-09-03（v0.6.0 云任务方案完成两轮评审修订，等待最终复审后开工）

---

## 一、现在是什么状态

SaveLink 当前 v0.5.0 基线已经完成验证，v0.6.0 处于云冲突治理最终复审阶段，业务实现尚未开始。动态发现阶段 1 至 5 已完成：待配置游戏可由 SaveLink 启动并监测，退出后由用户确认候选目录，再接管现有自动备份。`Hole Is Mine` 完整闭环、RUNE《暗黑地牢》目录归并和 `HowManyDudes` 主存档定位均已通过真实绿色版验证。云端存档的重复请求冲突、长时间读取和失败假空问题已完成修复与真实百度只读回归；两轮设计评审意见已经吸收，最终复审通过后以统一云任务协调器替代拒绝式全局互斥。

核心闭环已经可用：

```text
Steam 自动发现 / 游戏程序识别 / DeSmuME 扫描 / 手动添加 -> 绑定存档来源 -> 创建快照 -> 时间线 -> 恢复
```

MVP 后第一轮补齐也已完成：

- 编辑游戏名称和存档路径。
- 移除游戏（不删除真实存档目录）。
- 删除/锁定/备注快照。
- 打开快照详情时异步比较当前真实存档与指定快照，显示一致、不一致或无法检查；复用创建快照的指纹算法，全程只读且不写数据库。比较不获取包含云端网络请求的快照写锁，前端超过 15 秒会停止转圈并报告超时。
- 存档目录缺失时支持“创建目录并恢复”。
- 恢复失败页按错误类型给出更准确文案。
- 版本号已移到首页 SaveLink 标题旁；顶部齿轮打开设置页，第一个正式设置项为默认开启的全局“自动备份”开关。
- `get_app_info` 和打开/复制 AppData 路径的底层实现仍保留，当前设置页不展示内部路径；Tauri opener ACL 继续限制在 AppData。
- SaveLink 云链 V9 已替换网页、窗口、任务栏、托盘和安装包所需图标资产及配置；窗口/任务栏显示已实机确认，托盘和安装包继续纳入发布回归。
- Tauri 启动时执行 `startup_self_check`，清理上次异常中断留下的 Writing 半成品，并续做云端已删后中断的 Deleting 本机快照。
- 打包脚本可构建绿色版、NSIS、MSI。
- 百度网盘 OAuth、应用目录、元数据/快照上传下载和 sha256 校验 POC 已通过。
- 2MiB 基准测试已确认百度后端使用单快照 zip + `.ok`；本地快照仍由 `FsStore` 以目录形式保存在 `repository` 中。
- 云同步本机基础设施已实现：4 张新表、`CloudStateRepository`、`CloudObjectStore`、文件系统 `FakeCloudObjectStore` 和 G 组测试。
- 协议 JSON、`ZipCloudArchiveCodec` 和 `CloudSyncService` 已实现；设备 A -> Fake 云端 -> 设备 B 的上传、发现、下载、接收落地闭环已通过 H 组测试。
- 正式 `BaiduNetdiskStore` 已实现：逻辑路径映射、流式单步上传、CreateOnly/Overwrite、分页列表、stat、filemetas/dlink 下载、幂等删除、错误分类和临时下载清理均已接入。
- I 组本地 HTTP 契约测试全绿；J 组真实百度网盘上传、列表、下载和删除冒烟已执行通过，Token 仅通过环境变量注入。
- 正式客户端已实现百度 OAuth：系统浏览器授权、`127.0.0.1` 回调、随机 `state` 校验、授权码换 Token、本机 Token 文件持久化和 SQLite 账号引用登记均已实测通过。
- 快照上传按钮已调用 `CloudSyncService`：未授权时授权后续传，已授权时直接上传；入口使用云图标加文字，并明确显示 `上传`、`上传中`、`已上云`、`重试` 四种状态。
- `RefreshingBaiduTokenProvider` 会在 Token 临近过期时刷新并保存新凭据；授权失效时清除本机无效 Token，下一次点击重新授权。
- 2026-07-16 真实绿色版已把单条快照上传为同 ID 的 `.zip` 与 `.ok`，百度客户端和 SaveLink 页面均验收通过。
- 顶栏“云端存档”已接入真实百度发现和下载：只读发现不创建本机游戏；下载通过 zip 与内容指纹双重校验后，才写入设备 B 的 `repository`/SQLite 并创建未绑定游戏。
- 云端发现按最多 8 个游戏受控并发；已验证的不可变 `.ok` 复用本机记录，zip 继续检查目录条目和大小，metadata 仅在远端 `server_mtime` 变化时下载。前端同一发现请求使用 single-flight，关闭重开时显示会话缓存并接回原请求；真实百度约 12 个游戏、80 余快照首次读取约 6.846 秒，关闭重开约 543 毫秒恢复列表，无冲突、延迟错误或假空状态。
- 设备 B 隔离模式使用 `%APPDATA%\com.daiq.savelink-device-b-test`，顶栏显示测试标识；未绑定本机存档目录时创建快照和恢复均禁用。
- 未绑定游戏显示独立“绑定存档目录”主按钮；弹窗选择目录后自动只读扫描，扫描成功才允许确认。确认只更新本机路径，不自动创建快照、恢复或上云。
- 设备 B 已使用假目录完成绑定和手动恢复，恢复文件与下载到本机仓库的目标快照 SHA-256 一致。
- 2026-07-23 代强和朋友已用两台真实电脑、非开发者普通百度账号跑通授权、上传、发现、下载、绑定和恢复；普通用户无需创建百度应用，也无需预先打开百度网盘客户端。
- 2026-07-28 第一版移除恢复前自动保护点：恢复不会新增或复用快照，确认页提醒用户可先手动创建快照；`.old` 保留到最终校验通过，失败时自动换回原目录。旧 `before_restore` 数据继续兼容读取。
- 2026-08-05 已发布 GitHub Release `v0.1.0`，包含绿色版 ZIP 和 SHA-256；GitHub 是主仓库，Gitee 是国内镜像，双方代码和标签一致。
- 当前发布 ZIP 生成时间早于 `v0.1.0` 中最后一个应用代码提交“托盘增加划线”，需从标签对应提交重新打包、复测并替换 Release 附件。
- v0.2.0 已接入启动立即检查和每 10 分钟后台检查；内容不变不创建，单游戏失败不阻断其他游戏，后台事件会刷新前端时间线。
- 自动创建且校验成功的 `reason=auto` 快照立即尝试上传；未授权时不弹浏览器，授权失效/断网/限流时保留本地并在后续周期重试。历史手动快照不会被后台批量上传。
- 每个游戏默认统一保留 10 条未锁定记录，设置页可配置为 1 到 100 条；手动/自动/云端接收均计入，锁定记录不计入且数量不限。已有游戏的旧数据库在用户确认新策略前保留原有超出快照。
- 无备注快照按本机显示区域动态命名：普通区最新为“存档1”，锁定区最新为“锁定存档1”，锁定区整体置顶。锁定/解锁只立即改变保护标识，原区域保持到十分钟维护并显示待整理；关闭自动备份只停止创建，不停止整理、清理和云端收尾。
- 联合清理顺序为云端 `.ok` -> `.zip` -> 本地快照文件 -> `snapshots` 记录 -> 云同步缓存。云删除失败写 `delete_failed` 并保留全部本地数据；旧 v0.1.0 数据库会无损迁移新状态约束。
- 快照持久化时间已统一为秒精度 UTC RFC 3339；旧本机时间和带偏移云端时间会自动迁移，前端按本地时区显示。最近快照、去重和保留数量淘汰不再依赖混合格式字符串排序，已有协议 v1 云对象保持兼容。
- Steam 自动发现已接入“添加游戏”：从注册表定位 Steam、枚举游戏库和 appmanifest，并用随包 `manifest.db` 匹配 Ludusavi 规则；手动添加入口继续保留。
- 游戏程序识别已接入“添加游戏”：面向用户只允许选择 Windows `.lnk` 或 EXE，所选程序附近的 AppID 只有在 Manifest 游戏名与程序/快捷方式/目录身份完整相等时才采用；冲突时保留诊断提示、忽略该 AppID 身份和存档规则，再执行保守名称回退。底层目录扫描能力仍保留供内部复用，但“游戏目录”直接添加入口已移除。
- 普通 PC 游戏新建时必须保存本机启动绑定：普通游戏保存真实 EXE/安装目录，Steam 保存 `steam.exe + -applaunch AppID`；DeSmuME 继续使用模拟器/ROM 绑定。旧游戏再次添加时补绑原记录，不创建重复游戏。
- 待配置游戏可在没有存档目录时先登记，但创建、恢复、上传均被拒绝，自动备份会正常跳过；首页明确显示当前不会创建备份，并提供“启动游戏并查找存档”。
- 已配置且有本机启动绑定的普通游戏，首页提供“启动游戏”；该动作只启动 EXE 或 Steam，不创建监听会话、不记录 PID、不进入候选分析。已配置游戏的自动备份仍由启动检查和 10 分钟轮询负责。
- 动态发现使用原生 `ReadDirectoryChangesW` 多根监听、PID 生命周期和 5 秒退出宽限期；除标准用户目录外，还通过 Known Folder API 条件性监听公共文档下实际存在的 `Steam\RUNE`/`Steam\CODEX` 根目录。这两类已知根目录下如果紧跟纯数字 AppId，任意子树变化都归并为整个 `<AppId>` 游戏级候选，不再并列推荐 `remote`/`profile_*`/`backup`。其他未知路径仍保留通用父子候选逻辑。
- Edge、Chrome、Firefox、Brave、Vivaldi、Opera 等已知浏览器用户资料目录强制为低可信且不可确认，避免 Cookies、登录会话和浏览记录被误选；`.bak` 主备文件配对只作为辅助加分，不能单独构成游戏存档领域证据。
- 用户确认候选后，后端重新校验候选成员、安全范围、重复/嵌套和跨游戏冲突；自动备份开启时为当前游戏创建首份 `reason=auto` 快照，之后继续使用既有 10 分钟检查、自动上传和可配置保留策略。
- 多存档目录已贯通模型、SQLite、扫描、指纹、`FsStore`、恢复、云端协议、设备 B 绑定及前端展示；2026-08-10 使用《杀戮尖塔》真实候选和四个隔离假目录完成 A/B 快照往返恢复及移除安全验收。
- DeSmuME 支持已贯通模型、SQLite、精确文件扫描、`FsStore`、恢复、云端 ROM 身份和前端添加/重新绑定流程；只保护目标 `.dsv`，不保护 `.dsv-01` 至 `.dsv-09` 即时存档，不上传 ROM 或本机路径。
- Elden Ring 父子路径重叠已修复：末尾 `<storeUserId>` 只接受目录，Manifest 结果会收敛父子路径；手动添加/编辑及创建/恢复前也会拒绝重叠目录。修复后的真实开发版和绿色版均确认只保护数字用户目录，配置父目录只进入 `config_paths`。

当前客观状态：

- `savelink-core`：150 项通过；J/L/Q5 三项真实环境测试默认忽略，均已在相应验收阶段按需执行通过。
- `savelink-app`：Tauri 40 个测试全绿，React `npm run build` 和 core/Tauri 严格 Clippy 通过；启动绑定、防重、仅启动、原生监听、候选确认、首份自动快照、快照显示编号、云端元数据清理保护、增量发现缓存及云任务占用类型均有回归覆盖。
- `savelink-app`：Tauri 桌面应用，React 前端 + Rust 命令薄壳。
- 数据位置：`%APPDATA%\com.daiq.savelink\`。
- 产物位置：`savelink-app/src-tauri/target/release/`。
- v0.3.0 绿色版产物位置：`savelink-app/src-tauri/target-v030/release/bundle/portable/`；SHA-256 为 `0BA1B1309C778D7DA2EED0D5C033807E0D0E8CA9CB3EFD3380A1DDECFFB4FD70`。
- GitHub 主仓库：`https://github.com/daiqiang/save_link`。
- Gitee 国内镜像：`https://gitee.com/qigugu/save-link`。
- 当前发布候选：`v0.5.0`；已发布版本：`v0.1.0`、`v0.2.0`、`v0.3.0`、`v0.4.0`。程序识别、动态发现和本地一致性检查均纳入 v0.5.0。

**这是一个能跑、被测试保护、已经经过多轮人工验收的代码库，不是空白项目。不要推倒重来。**

## 二、必须先读的文档（按顺序）

1. `PROGRESS.md`：当前进度、已完成能力、产物位置、后续候选项。
2. `SaveLink当前功能完成情况.md`：给总规划会话看的当前状态摘要。
3. `SaveLink技术架构.md`：架构、数据模型、恢复事务设计、实现现状与偏差。
4. `SaveLink恢复与存储测试规格.md`：恢复/存储的验收基准。
5. `savelink-core/README.md` + `savelink-app/README.md`：两个模块的结构与铁律。
6. 产品三件套：
   - `SaveLink-MVP产品原型草案.md`
   - `SaveLink低保真原型图.md`
   - `SaveLink视觉与交互说明.md`
7. `SaveLink云端快照协议V1.md`：已定稿的云端目录、对象格式、发现、上传、下载、冲突和失败恢复协议。
8. `百度网盘API-POC报告-20260714.md`：OAuth/文件 API 实测、2MiB 基准数据和单快照 zip 决策证据。
9. `SaveLink-v0.4.0-DeSmuME模拟器支持方案.md`：当前 DeSmuME 范围、ROM 身份、精确 `.dsv` 保护、跨设备恢复和验收边界。
10. `SaveLink游戏启动与存档动态发现方案.md`：当前主线的产品流程、启动绑定、监听/PID 设计、实施阶段和验收边界。

## 三、不可触碰的红线

1. **不丢存档是最高优先级。**
   任何会写、覆盖、删除真实存档目录的逻辑，改动前后都要让 `savelink-core` 的 B 组、E 组测试全绿。恢复必须遵守“目标快照校验成功才覆盖 + 同盘原子 rename + 失败回滚”。

2. **不准改测试来迁就实现。**
   测试是验收尺。允许新增测试，不允许削弱既有断言。不准绕过 `FailingStore` 故障注入。

3. **不准破坏存储抽象。**
   上层只认不透明 `storage_key`。换 zip/restic 就新实现 `SnapshotStore`，换数据库就新实现 `Repository`，service 与测试不动。

4. **前端不准在组件里直接 `invoke`。**
   一律走 `savelink-app/src/lib/api.ts`。

5. **不准把内部实现概念暴露给用户。**
   Kopia、Ludusavi、repository、manifest、storage_key 等不应直接出现在用户界面。

6. **不要向用户暴露内部错误堆栈。**
   面向用户的文案要说清楚“发生了什么、有没有动真实存档、下一步怎么办”。

7. **Tauri 权限要最小化。**
   例如 opener 目前只允许打开 `$APPDATA` 与 `$APPDATA/**`。不要为了方便开放整个磁盘。

## 四、每次改动的标准动作

- 改了 `savelink-core`：
  ```text
  cd savelink-core
  cargo test --no-fail-fast
  ```
  必须全绿。

- 改了前端：
  ```text
  cd savelink-app
  npm run build
  ```
  必须通过。

- 加了端到端功能：
  1. `savelink-core` 加逻辑并补测试。
  2. `savelink-app/src-tauri/src/commands.rs` 包 DTO / Tauri 命令。
  3. `savelink-app/src-tauri/src/lib.rs` 的 `generate_handler!` 注册命令。
  4. `savelink-app/src/lib/api.ts` 加调用函数。
  5. 组件只调用 `api.ts`。
  6. 用真实 Tauri 窗口或绿色版验证。

- 改了打包相关：
  ```text
  cd savelink-app
  .\build-installer.bat
  ```

- 完成后更新：
  - `PROGRESS.md`
  - 必要时更新 `savelink-app/手动测试计划.md`
  - 如果影响架构或安全边界，更新 `SaveLink技术架构.md` / `SaveLink恢复与存储测试规格.md`

## 五、当前开发方向

### 已完成：v0.2.0 自动备份

权威方案位于 `SaveLink-v0.2.0自动备份方案.md`。当前实现：全局开关默认开启；启动立即检查，之后每 10 分钟运行维护周期；自动快照校验后立即上云；手动、自动和云端接收的未锁定记录统一计入可配置上限，默认 10、范围 1 到 100；锁定记录不计入且数量不限；旧记录按云端优先顺序与本地联合清理，云删除失败保留本地等待重试。普通区/锁定区分别动态编号，锁定或解锁后的区域移动延迟到维护周期；关闭自动创建时维护仍继续。

本地专项、真实百度主流程和设备 B 时间修复均已验收，v0.2.0 可以进入发布。以下项目明确后置，不阻塞本次发布：真实断网重试、Token 失效/限流、真实云删除中途失败、另一设备提前删除对象，以及多设备冲突选择交互。自动任务仍不得弹授权页或后台覆盖多设备冲突。

### 并行待办：v0.1.0 发布后加固

项目已明确不单独发布 v0.1.1。以下事项保留但不阻塞 v0.2.0 设计与开发：

1. 两台真实电脑首次验收已通过，继续长期自用观察。
2. [已完成] 非开发者普通百度账号完整授权与同步，且不依赖百度网盘客户端运行。
3. 断网、授权失效、传输中断和重试回归。
4. [部分完成] 自定义应用图标、版本号和 GitHub Release 已完成；继续清理正式包测试标识并回归安装/托盘显示。
5. Token 安全存储与百度应用审核/权限准入。
6. [已完成] 创建 `v0.1.0` 标签、发行说明和绿色版 Release。
7. [待处理] 从 `v0.1.0` 标签对应提交重新生成绿色版 ZIP，复测后替换 GitHub Release 附件。
8. [待处理] 用 v0.1.0 当前界面替换公开 README 的旧截图；旧图含已删除的自动恢复前备份，不得继续作为当前界面展示。

更新进度时以 `PROGRESS.md` 和 `SaveLink-v0.2.0自动备份方案.md` 为准，避免多份文档状态漂移。

### P2：已完成 - 启动自检接入 Tauri 启动路径

Tauri setup 已在 `AppState::init` 后、`app.manage(state)` 前调用 `startup_self_check`，同时处理 Writing 创建残留和 Deleting 删除残留。后续正式回归时，可补真实窗口场景验收。

### P3：真实恢复进度事件

core 已有 progress 回调概念，当前 Tauri 命令传空回调。可改为通过 Tauri event 向前端推送步骤进度。

### P4：真 zip / restic 存储

当前 `FsStore` 是把快照目录树写入 `repository` 的 `SnapshotStore` 实现，不压缩。若要降低空间占用和快照文件数量，新增 `ZipStore` 或接 restic。不要让 service 解析具体存储结构。

### P5：安装体验

SaveLink 云链 V9 自定义图标已生成到 `src-tauri/icons/`，网页 favicon、窗口、任务栏、托盘和安装包配置均已接入，窗口/任务栏显示已由代强实机确认。剩余工作是正式包工程测试标识清理，以及在重新打包的 v0.1.0 产物中复测托盘和安装包图标。

### P6：已完成 - Steam 自动发现与多存档目录

Steam 候选发现和多目录主链路已接入并通过自动测试与真实绿色版验收。扫描和快照会合并所有根目录；仓库与云端归档保留根目录边界；恢复会替换每个根目录，任一路径失败时回滚全部已替换目录；编辑、恢复确认、快照详情、设备 B 绑定和移除确认均展示完整路径列表。Elden Ring 的 `<storeUserId>` 通配符误匹配同级配置文件问题已修复，自动发现会收敛父子路径，所有写入和恢复入口也会拒绝重叠来源。

### P7：已完成 - v0.4.0 发布

DeSmuME 代码主链路已完成：扫描模拟器和 ROM、解析 Header/SHA-256、精确选择 `.dsv`、保存 ROM 身份、SQLite 迁移、云端身份兼容、添加/重新绑定 UI，以及目标文件级快照和回滚恢复。ROM 目录扫描已改为带 canonical 路径去重的迭代遍历，避免 junction/目录链接环递归；ROM SHA-256 的 1 MiB 流式缓冲区已移到堆上，避免 Tauri 工作线程在首次读取 ROM 时栈溢出。v0.4.0 发布阶段的 109 个 core 测试、前端构建和真实样本 Q5 只读发现均通过；当前总基线见本文开头。

用户已确认真实 ROM 目录扫描不再闪退，v0.4.0 标签与 Release 已正式发布；Yuzu 留到后续版本。

### P8：已完成 - 游戏程序与快捷方式识别验收

核心 `program_discovery.rs` 支持目录、EXE 和 Windows `.lnk`；当前 UI 只允许选择 `.lnk` 或 EXE，目录扫描保留给内部回归。快捷方式通过 Shell COM 在独立 STA 线程只读解析，扫描最多进入安装目录 3 层、512 个目录，不遍历其他磁盘。身份优先读取 `steam_appid.txt`、`steam_emu.ini`、`SmartSteamEmu.ini`、`ColdClientLoader.ini`、`configs.app.ini`；AppID 对应的 Manifest 游戏名必须与快捷方式、EXE 或目录身份至少有一项完整相等，否则忽略该 AppID 和对应规则。没有可靠 AppID 时也只接受完整名称相等。命中游戏后复用 `steam_discovery.rs` 的 Manifest 规则展开与路径分类。

2026-08-18 使用 `Arcane.Trigger.Build.21206983` 完成真实绿色版只读验收。首次验收发现其目录名称被错误按前缀命中 Manifest 中的 `Arcane / 534870`；移除前缀匹配并增加回归测试后，桌面快捷方式、`GunWizard.exe` 和安装目录三种入口均安全返回 0 个匹配，“添加游戏”禁用。程序未启动，既有手动添加记录和真实存档未修改；手动添加继续永久保留为最终兜底。

### P9：当前主线 - 游戏启动与存档动态发现

权威设计是 `SaveLink游戏启动与存档动态发现方案.md`。阶段 1 至 5 已完成。当前模型用仅本机的 `launch_binding` 保存普通 EXE 或受控 Steam 启动方式，不进入云协议；Steam AppID、EXE/安装目录和存档路径共同参与跨入口防重。

2026-08-25 已在备份后事务修复本机正式数据：为奥术扳机补 `GunWizard.exe`，为艾尔登法环和杀戮尖塔 2 补 Steam AppID，删除零快照且无云绑定的重复 `GunWizard`。修复没有操作真实存档/快照文件，SQLite 完整性为 `ok`，25 个快照无孤儿。历史验收数据没有真实 EXE，因此没有伪造绑定。

正式工程已接入原生监听、全局单会话、受控启动、PID 退出/宽限期、候选排序、人工确认和首次自动快照。最终真实样本捕获 507 条变化、生成 12 个候选，正确 `Release` 目录为唯一高可信；首份快照 6 个文件、39,670 字节与源目录哈希一致。详细结论见 `SaveLink游戏启动与存档动态发现方案.md`，本机过程记录保留在未入库的 `acceptance-data/save-discovery-stage5-20260825/`。

2026-08-28，`HowManyDudes.exe` 携带的错误 AppID 不再误命中 `DrainSim`，真实主存档 `AppData\Local\HowManyDudes\<用户编号>\Game` 已进入高可信候选；浏览器资料目录强制低可信且不可确认。游戏自带 `Backup\<用户编号>\Game` 仍可能并列高可信，后续可优化提示或排序，但当前必须由用户核对并只选择主存档。

2026-08-26 《暗黑地牢》RUNE 学习版暴露公共文档根目录不在监听范围：真实文件位于 `Public Documents\Steam\RUNE\262060`，游玩期间持续变化但候选完全缺失。实现已条件性加入已存在的 RUNE/CODEX 根目录，并将数字 AppId 下所有子树变化归并到整个 `<AppId>` 目录。RUNE 样本证明 `filemappings.ini` 位于 `remote` 外且随进度变化，因此保护边界不能缩到 `remote`。用户使用新绿色版真实游玩并退出后，界面只推荐 `C:\Users\Public\Documents\Steam\RUNE\262060`，未并列推荐 `profile_*`/`backup`，真实复验通过（未留截图）。CODEX 按相同标准布局完成自动回归，但尚无本机真实样本。

### P10：自动化 / 游戏识别 / 云端

- v0.2.0 自动备份已进入当前 P0：全局开关默认开启；启动时立即检查，之后每 10 分钟检查变化，不依赖游戏退出；每个游戏默认保留最近 10 个未锁定记录，可在设置页配置为 1 到 100 个，锁定记录在此之外且不设数量上限；最终同步清理本地与百度网盘旧记录。
- 游戏识别已使用随包 Ludusavi Manifest SQLite 数据库接入 Steam 自动发现和用户指定程序识别；程序识别自动化及真实学习版样本只读验收均已通过。
- 云端原则：同步 SaveLink 的共享快照事实和云端元数据，绝不直接同步真实存档目录。
- 不把 `latest.zip` 或整份 `savelink.db` 原样同步作为云端主线；主线是云端目录结构 + 增量同步。
- 百度网盘快照物理格式已定为每条快照一个 `{snapshot_id}.zip`，zip 上传成功后再生成并上传云端 `{snapshot_id}.ok`。
- 云端快照协议 v1 已定稿：按游戏目录发现，不使用全局 `snapshots.json`；有效 `.ok` 是不可变发布记录。
- v1 `.ok` 保持不可变；上传后的用户名称/锁定状态通过 `snapshot-meta/{snapshot_id}.json` 按字段修改时间跨设备合并。同一时刻名称保留云端值、锁定取 `true`；断网可重试，元数据未同步时禁止云端清理。
- 十分钟后台维护只主动重试 `pending/error` 元数据，并执行增量发现以看见另一设备的新锁定状态；不再全量下载已同步 `.ok` 和 metadata。
- v0.6.0 当前权威设计为 `SaveLink-v0.6.0云任务调度与冲突治理方案.md`：先引入内存协调器和应用级共享百度 Runtime，再将上传、发现、下载、metadata 和删除全部入队；同快照普通任务 FIFO，删除取代未运行或等待中的任务，运行请求结束后再删。跨设备删除使用不可变 tombstone，上传、接收、metadata 和恢复检查均受其约束；SQLite 业务状态负责跨重启恢复。方案已完成两轮评审修订，最终复审前不要开始业务实现。
- `app_settings`、`cloud_accounts`、`cloud_game_bindings`、`cloud_snapshot_sync` 已落库；旧数据库打开时自动补表并补充 `remote_metadata_modified_at`。
- 协议 JSON、`CloudArchiveCodec` 和 `CloudSyncService` 已实现，Fake 云端已完成“上传 -> 发现 -> 下载 -> 双重校验 -> 写入另一套本机 repository -> 登记 SQLite”闭环。
- 云同步术语统一为“接收云端快照并落地到本机快照仓库”；“导出/导入”只用于未来独立的手动备份功能。
- `BaiduNetdiskStore`、OAuth、自动刷新、真实上云、设备 B 接收、独立绑定和安全恢复已完成实测，构成 v0.2.0 自动上传与冲突处理的现有基础。
- Token 当前保存在 `%APPDATA%\com.daiq.savelink\credentials\baidu-oauth.json`，SQLite 只保存引用；v0.1.0 已按明文文件状态发布，凭据加密是 v0.1.x 优先安全项，账号解绑/切换仍是低频增强。
- 手动 zip 导出/导入只作为备份迁移工具，当前优先级低。

## 六、有疑问时

- 涉及“会不会丢用户存档”的任何判断：停下来问总规划会话。
- 拿不准是不是该改测试：默认不改，问总规划会话。
- 产品行为/文案拿不准：先查产品三件套；仍不清楚再问。
- 文档与代码不一致：以代码和测试为准，同时更新文档。
