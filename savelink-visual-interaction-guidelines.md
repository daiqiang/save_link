# SaveLink 视觉与交互说明

## 版本历史

| 版本 | 修改人 | 时间 | 备注 |
| --- | --- | --- | --- |
| 1.0 | 代强 | 2026-06-18 | 第一版：定义 SaveLink MVP 的视觉气质、交互原则、页面布局和实现约束 |
| 1.1 | 代强 | 2026-07-08 | 同步当前实现：快照存储为 FsStore 目录复制，zip/restic 后置 |

## 文档用途

本文档用于指导 SaveLink MVP 的前端实现。

配套文档：

- `savelink-mvp-product-prototype.md`：定义产品流程和功能范围。
- `savelink-low-fidelity-wireframe.md`：定义低保真页面结构。

本文档不重复功能需求，只规定界面应该呈现出的产品气质、视觉密度、交互层级和安全感。

## 技术栈方向

第一版建议技术栈：

```text
桌面壳：Tauri
前端：React + TypeScript
样式：Tailwind CSS
组件：Radix UI / shadcn/ui 风格组件
图标：lucide-react
本地逻辑：Rust command
本地数据：SQLite
快照文件：当前 FsStore 目录复制；zip/restic 或单快照 zip 为后续存储/云端优化方向
```

实现时可以先用 mock 数据完成前端页面和交互，再接入真实 Tauri/Rust 命令。

## 产品气质

SaveLink 应该像一个安静、可靠、专业的桌面工具。

关键词：

- 安静。
- 稳定。
- 可信。
- 清晰。
- 可扫描。
- 有安全感。
- 面向长期使用。

不要做成：

- 营销落地页。
- 游戏社区首页。
- 网盘首页。
- 花哨启动器。
- 大面积渐变卡片应用。
- 大留白、大标题、英雄区风格页面。

SaveLink 的第一印象应该是：

> 这是一个认真保护我游戏存档的工具。

## 布局原则

### 主界面

采用固定的桌面工具布局：

```text
左侧游戏列表
右侧游戏详情
顶部轻量工具栏
```

建议：

- 左侧栏宽度约 `260px - 300px`。
- 顶部栏高度约 `48px - 56px`。
- 右侧详情区用于展示当前游戏的时间线。
- 页面整体不需要 hero 区。
- 不使用嵌套卡片。
- 不把整个页面包在一个大卡片里。

### 信息密度

SaveLink 是工具，不是展示页。

界面应保持中等偏高信息密度：

- 列表项紧凑但可读。
- 时间线卡片不要过高。
- 标题不要过大。
- 按钮不要过大。
- 重要状态应清楚，但不要用大面积色块。

建议字体层级：

- 页面标题：`20px - 24px`。
- 区块标题：`15px - 17px`。
- 正文：`13px - 14px`。
- 辅助信息：`12px - 13px`。

不要使用 viewport 相关字体缩放。

## 色彩方向

整体采用中性浅色工具风格。

建议：

- 背景：接近白色或浅灰。
- 左侧栏：浅灰或微弱分区色。
- 主强调色：蓝色或青蓝色。
- 成功状态：绿色，但少量使用。
- 警告状态：琥珀色。
- 危险状态：红色，只用于删除、覆盖风险、失败。

不要：

- 大面积紫色渐变。
- 大面积深色背景。
- 大面积游戏风装饰。
- 彩色玻璃拟态。
- 霓虹感。
- 复杂纹理背景。
- 装饰性光斑或渐变球。

恢复操作不应全程红色恐吓。只有真正危险的删除、失败、不可逆动作才使用红色。恢复确认弹窗里可以用警告色提醒“会覆盖当前真实存档”，同时强调“已先自动备份”带来的安全感。

## 组件原则

### 按钮层级

至少区分四类按钮：

- 主按钮：创建快照、添加游戏、确认恢复。
- 次按钮：编辑游戏、取消、修改备注。
- 危险按钮：删除快照、删除游戏。
- 图标按钮：更多、设置、关闭、锁定。

按钮文案要具体：

- 好：`备份当前存档并恢复`
- 差：`确定`

### 图标

使用 `lucide-react` 图标。

建议：

- 添加游戏：`Plus`
- 创建快照：`Camera` 或 `Archive`
- 恢复：`RotateCcw`
- 锁定：`Lock`
- 取消锁定：`Unlock`
- 更多：`MoreHorizontal`
- 设置：`Settings`
- 文件夹：`Folder`
- 警告：`TriangleAlert`
- 成功：`CheckCircle`

图标按钮需要 tooltip。

### 卡片

只在这些地方使用卡片：

- 游戏列表项。
- 快照时间线项。
- 弹窗。
- 抽屉。

不要：

- 页面套页面卡片。
- 卡片里面再套卡片。
- 每个小信息都做成卡片。

卡片圆角建议 `6px - 8px`。

### 弹窗

弹窗用于高风险或短流程操作：

- 添加游戏。
- 恢复确认。
- 删除确认。

弹窗宽度建议：

- 添加游戏：`560px - 680px`。
- 恢复确认：`520px - 600px`。

弹窗内容应简洁，不要写成说明书。

### 抽屉

快照详情使用右侧抽屉。

建议：

- 宽度 `380px - 460px`。
- 展示快照元数据、备注、操作。
- 不展示过长文件列表。

## 页面细则

### 空首页

目标：引导用户添加第一个游戏。

要求：

- 空状态居中。
- 只放一个主要行动按钮：`添加游戏`。
- 不要大插画。
- 不要写长篇功能介绍。
- 不要放营销式标题。

推荐文案：

```text
还没有添加游戏
添加一个游戏后，就可以为它创建存档快照和时间线。
```

### 添加游戏弹窗

目标：让用户快速完成第一步配置。

要求：

- 游戏名称在上。
- 存档目录选择是核心。
- “测试读取”结果要即时显示。
- 快照仓库默认位置不要打扰用户。
- 保存按钮文案使用 `保存并创建`。

目录读取结果示例：

```text
已检测到：3 个文件，12.4 MB
```

异常示例：

```text
无法访问该目录，请重新选择。
```

### 游戏详情页

目标：让用户围绕时间线完成核心操作。

要求：

- 游戏名称清楚。
- 当前存档目录清楚。
- `创建快照` 是页面主按钮。
- 时间线是视觉中心。
- 每个快照项必须有时间、备注、文件数量/大小、恢复入口。
- “恢复前自动备份”需要有特殊标记，但不要刺眼。

快照项建议结构：

```text
时间
备注
文件数量 / 大小 / 状态
[恢复] [更多]
```

### 快照详情抽屉

目标：查看和管理某个快照。

要求：

- 显示时间、备注、文件数量、大小、创建原因、锁定状态。
- 主操作为 `恢复这个版本`。
- 删除操作放在次要位置，并使用危险样式。
- 锁定快照不能直接删除。

### 恢复确认弹窗

目标：让用户放心执行恢复。

要求：

- 标题明确：`恢复到这个存档版本？`
- 明确目标游戏。
- 明确目标时间点。
- 明确当前存档会先自动备份。
- 按钮文案必须是 `备份当前存档并恢复`。
- 如果自动备份失败，恢复必须中止。

推荐结构：

```text
SaveLink 将执行以下操作：

1. 先自动备份当前真实存档。
2. 再把目标版本恢复到游戏存档目录。
3. 恢复完成后，你仍然可以恢复“恢复前自动备份”。
```

恢复进行中要显示步骤状态：

- 已备份当前存档。
- 正在恢复目标版本。
- 正在校验恢复结果。

恢复完成后要告诉用户自动备份在哪里：

```text
恢复前的当前存档已自动保存为：
2026-06-18 22:05 恢复前自动备份
```

## 文案原则

文案要像工具，不像广告。

推荐用词：

- 添加游戏。
- 创建快照。
- 时间线。
- 恢复这个版本。
- 恢复前自动备份。
- 存档目录。
- 快照仓库。

谨慎使用：

- 仓库。
- 源路径。
- 目标路径。
- 增量。
- 哈希。
- 去重。
- Manifest。

这些词可以出现在高级设置或开发文档里，不应出现在普通用户主流程里。

错误文案要告诉用户下一步能做什么。

示例：

```text
无法访问该存档目录。请确认目录仍然存在，或重新选择存档目录。
```

## 状态设计

至少实现这些状态：

- 空状态。
- 正常状态。
- 创建快照中。
- 快照创建成功。
- 存档未变化。
- 快照创建失败。
- 恢复确认。
- 恢复中。
- 恢复成功。
- 恢复失败。
- 存档目录不存在。
- 快照已锁定。

恢复失败必须说明：

- 当前存档是否已经被备份。
- 恢复是否已经开始覆盖。
- 用户下一步应该怎么处理。

## 安全感原则

SaveLink 的关键体验不是“快”，而是“放心”。

因此：

- 恢复前必须自动备份当前存档。
- 恢复动作必须二次确认。
- 删除锁定快照必须先取消锁定。
- 恢复失败不能留下用户完全不知道发生了什么的状态。
- 不要静默覆盖。
- 不要静默删除。
- 不要让用户手动选择恢复目标目录作为主流程。

## 实现给 Claude Code 的约束

如果把本文档交给 Claude Code 或其他 coding agent，实现时应明确要求：

- 不要做 landing page。
- 不要做 hero 区。
- 不要做营销式首页。
- 不要大面积渐变。
- 不要嵌套卡片。
- 不要把恢复按钮做成普通无风险按钮。
- 不要省略恢复前自动备份流程。
- 不要让普通用户看到 Kopia、Ludusavi、manifest、repository 等内部概念。
- 优先完成页面结构和交互状态，再优化视觉细节。
- 先用 mock 数据实现完整前端闭环。

## 验收标准

第一版页面验收时，至少检查：

- 用户能在 10 秒内找到“添加游戏”。
- 用户能在游戏详情页一眼看到“创建快照”。
- 用户能理解时间线里的每一条是一个可恢复版本。
- 用户点击恢复前，明确知道当前存档会被自动备份。
- 恢复完成后，用户能看到“恢复前自动备份”已经生成。
- 页面没有营销感。
- 页面没有过度装饰。
- 页面信息密度适合桌面工具。
- 按钮层级清楚。
- 删除、恢复等高风险操作有确认和状态反馈。

## 当前结论

这份视觉与交互说明的目标，是让实现出来的 SaveLink 第一版看起来像一个可靠的桌面工具，而不是临时脚手架页面。

后续实现时，建议把以下三份文档一起交给 coding agent：

```text
savelink-mvp-product-prototype.md
savelink-low-fidelity-wireframe.md
savelink-visual-interaction-guidelines.md
```

三份文档分别回答：

- 做什么。
- 页面怎么组织。
- 界面应该是什么气质。
