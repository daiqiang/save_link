# SaveLink 回归验收报告 - 2026-07-02

## 结论

本轮按总规划建议做了当前 MVP 的重点回归验收，并补验了新增的 `TC-41 启动自检清理上次中断残留`。

结论：未发现阻塞问题，可以进入下一功能阶段。建议下一阶段优先做“真实恢复进度事件”。

## 测试基线

- 分支：`master`
- 最新提交：`c9e057b fix: run startup self check on app launch`
- 运行方式：当前源码的 Tauri dev 窗口
- 测试数据目录：`C:\Users\daiqiang\door\project_workspace\save_link_workspace\acceptance-data\formal-regression-20260702`
- 用户数据目录：`C:\Users\daiqiang\AppData\Roaming\com.daiq.savelink`

## 自动验证

- `savelink-core`：`cargo test --no-fail-fast` 通过，34 个集成测试全绿。
- `savelink-app`：`npm run build` 通过。
- 说明：`npm run build` 在普通沙箱中曾因父目录读取权限失败，提升权限后通过；这是运行环境限制，不是代码失败。

## 真实窗口回归通过项

- 启动应用：窗口正常打开，标题为 `SaveLink`。
- 添加游戏：填写测试存档目录、测试读取、保存并创建均通过。
- 创建快照：首次快照成功；修改文件后再次创建快照成功。
- 恢复旧快照：恢复成功，并生成 `恢复前自动备份`。
- 替换语义：恢复旧快照后，目标快照没有的 `extra.sav` 被移除，确认不是合并式恢复。
- 恢复 `恢复前自动备份`：可回到恢复前状态。
- 设置页：版本、用户数据目录、仓库目录、数据库路径显示正常；复制路径、打开数据目录、打开仓库目录均正常，无 Tauri opener 权限错误。
- 编辑游戏：修改游戏名和存档路径后保存成功，详情页显示新名称和新路径。
- 缺失目录恢复：
  - 触发后进入 `存档目录不存在` 选择页。
  - 点击 `取消` 后不创建目录、不写入文件。
  - 点击 `创建目录并恢复` 后目录被重建，目标快照内容恢复进去，并显示 `恢复完成`。
- 坏快照恢复：
  - 临时破坏 `.ok` 标记后执行恢复。
  - 失败页显示 `快照已损坏` 和 `本次恢复未修改你的真实存档。`
  - 测完后已恢复被临时破坏的 `.ok` 标记。
- 存档目录不存在时创建快照：
  - UI 提示 `存档目录不存在`。
  - 数据库未新增快照。
  - 磁盘上没有创建该不存在目录。

## TC-41 启动自检

构造方式：

- 在 `savelink.db` 中插入一条 `status = writing` 的测试快照记录。
- 在仓库 `repository\snapshots` 下创建同名半成品快照目录和 `.ok` 标记。
- 重启当前源码 Tauri dev 窗口。

结果：

- 应用正常启动。
- 时间线未显示该 `writing` 残留快照。
- 数据库中的 `writing` 记录被清理。
- 仓库半成品目录和 `.ok` 标记被清理。
- 最终检查 `writing_count = 0`，无 `tc41_dangling_*` 残留。

结论：`TC-41` 通过。

## 未完成 / 待授权项

- `TC-17 移除游戏但不删除真实存档` 已走到确认弹窗，并确认弹窗文案包含 `不会删除真实存档目录`。
- 由于这是通过 Windows UI 执行删除类动作，需要用户明确授权后才能点击 `确认移除`。
- 因此本轮未点击最后确认；这不是产品阻塞，但建议授权后补测一次。

## 测试残留

- 工作区存在未跟踪测试数据目录：`acceptance-data/formal-regression-20260702/`。
- 该目录是本轮验收数据，不建议提交。
- 当前没有发现真实测试存档被错误删除或错误覆盖。

## 建议

当前 MVP 基线可认为稳定。若补完 `TC-17` 后仍无异常，建议进入下一阶段：真实恢复进度事件，把后端已有的备份当前存档、恢复临时目录、替换真实目录、校验完成等阶段 emit 到前端。
