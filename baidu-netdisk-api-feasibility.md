# 百度网盘 API 技术可行性调研

## 版本历史

| 版本 | 修改人 | 时间 | 备注 |
| --- | --- | --- | --- |
| 1.0 | 代强 | 2026-05-21 | 第一版：整理百度网盘开放平台接入认知、关键限制、用户免建应用判断，并将完整 API POC 调整为正式开发前验证项 |
| 1.1 | 代强 | 2026-07-08 | 同步云同步草案：POC 改为验证目录镜像与单快照 zip，不再默认 latest.zip |

## 背景

第一轮市场机会调研已经确认：游戏存档时间机器项目值得做，云端方向应采用 API-first 的可插拔存储后端，而不是依赖某个网盘客户端的同步目录。

百度网盘是第一优先调研对象，原因是：

- 国内用户认知度高。
- 普通网盘空间较大，用户接受度高。
- 相比坚果云，没有免费用户 `1GB/月` 上传、`3GB/月` 下载这类小流量约束。
- 百度网盘同步空间不适合作为主路径，用户实测同步空间只有 `1GB`。
- 文件夹自动备份更像上传最新文件的通道，不能替代存档历史版本系统。

## 当前认识

百度网盘不能简单当作 NAS 使用。

NAS/SMB/WebDAV 更接近“远程文件系统”，应用可以相对直接地读写路径。百度网盘开放平台则是受控 API 服务，需要开发者按平台流程接入：

```text
注册登录百度账号
→ 完成实名认证
→ 创建应用
→ 接入 OAuth 授权
→ 调用网盘 API
→ 测试运行
→ 正式上线前提交应用审核
```

这意味着百度网盘更适合作为“对象存储后端”，不适合作为“透明远程硬盘”。

需要特别区分两个角色：

- 开发者：创建应用、拿 AppKey/SecretKey、配置授权、准备上线审核材料。
- 普通用户：只需要在 SaveLink 里点击“连接百度网盘”，登录/扫码并授权。

因此，当前官方 API 路线虽然需要创建应用，但不应该要求每个用户自己创建应用。正确的产品模式是 SaveLink 官方创建一个应用，所有普通用户授权这个应用访问自己的百度网盘。

老文章中“不用自己创建应用”的做法，多数是使用百度早期示例应用或公开 AppKey，例如 `pcstest_oauth`。这适合理解原理或临时测试，但不适合正式产品：

- 应用身份不属于我们，稳定性和权限不可控。
- 上传目录固定到示例应用目录，例如 `/apps/pcstest_oauth/`，产品名称和数据归属都不清晰。
- 旧 PCS 接口和 `response_type=token` 直接复制 access token 的方式不适合面向普通用户。
- 一旦示例应用、旧接口或公开 AppKey 被关闭，产品会整体失效。

对本项目来说，正确使用方式应该是：

```text
SaveLink 本地生成历史快照
→ 生成 manifest.json 和 hash
→ 调用百度网盘 API 增量上传快照对象和云端元数据
→ 另一台设备通过 API 拉取云端元数据
→ 下载新增快照对象
→ 校验 hash
→ 恢复到本地测试目录或真实存档目录
```

## 好消息与风险

好消息：

- 百度网盘开放平台文档显示，测试运行阶段可以直接使用 API，无需先提交正式上线审核。
- 用户已完成开发者注册并创建应用，应用层级的主要概念风险已经降低。
- 普通用户不需要创建百度开放平台应用；创建应用是 SaveLink 开发方的接入工作。
- 完整上传、下载、列表、hash 校验 POC 可以延后到正式进入云后端开发前再做。

风险：

- 正式上线需要应用审核，可能涉及应用名称、应用介绍、权限说明、隐私政策、测试账号、开发者承诺等材料。
- API 权限可能存在目录、配额、限流、风控、会员权益等限制。
- 用户授权体验需要验证，尤其是桌面端是否能做成登录/扫码授权，而不是让用户复制复杂参数。
- 如果只能写入 `/apps/应用名/`，需要确认这是否影响云端目录设计。
- 桌面客户端不适合明文内置 SecretKey；如果百度 OAuth 不支持适合桌面端的安全授权方式，可能需要一个轻量授权中转服务。

## POC 目标

第二轮调研目标不是做完整产品，而是验证百度网盘能否作为第一个云存储后端。

当前已经确认到“开发者可创建应用、普通用户理论上只需授权”的层面，因此不必立即继续做完整 API POC。等项目进入云端后端开发阶段，再验证以下链路即可。

最小 POC 链路：

```text
创建百度网盘开放平台测试应用
→ 完成 OAuth 授权
→ 上传 cloud-manifest.json / games.json / snapshots.json
→ 上传一个模拟快照目录
→ 上传一个单快照 zip
→ 列出云端快照
→ 下载指定快照目录或单快照 zip
→ 校验 hash
→ 恢复到本地测试目录
```

建议模拟目录：

```text
/apps/SaveLink/
  cloud-manifest.json
  metadata/
    games.json
    snapshots.json
  snapshots/
    snap_dir_case/
      snap_001/
      snap_001.ok
    snap_zip_case/
      snap_002.zip
      snap_002.ok
```

建议本地测试目录：

```text
D:\door\save_link_poc\
  local_save\
  cloud_download\
  restored_save\
```

## 必须验证的问题

- 是否可以顺利完成开发者实名认证和创建应用。
- 测试运行阶段是否确实可以直接使用 API。
- 正式上线审核的周期、材料、权限边界是什么。
- OAuth 授权是否能做成普通用户可接受的登录/扫码体验。
- 普通用户是否可以完全不接触开放平台，只通过 SaveLink 完成授权。
- 桌面客户端如何安全处理 AppKey/SecretKey、access token 和 refresh token。
- 是否只能写入 `/apps/应用名/`。
- 普通账号上传、下载、列目录、删除文件是否都可用。
- 小文件、目录镜像快照、单快照 zip、大文件快照是否都能稳定上传。
- 百度网盘列大量小文件目录是否高效，还是更适合单快照 zip。
- 分片上传流程是否清晰稳定。
- 下载速度是否足够，是否明显受会员权益影响。
- `refresh_token` 是否稳定，用户是否需要频繁重新登录。
- API 配额、错误码、限流和风控是否会影响实际产品化。

## 初步设计判断

百度网盘 API 后端不应该承载版本逻辑，只承载存取逻辑。

产品层负责：

- 游戏识别或手动配置。
- 快照生成。
- 快照元数据。
- 云端元数据维护。
- hash 校验。
- 恢复前备份当前存档。
- 冲突检测和分支保留。

百度网盘后端只负责：

- 上传对象。
- 下载对象。
- 列出对象。
- 删除对象。
- 查询对象元数据。

抽象接口可以先设计为：

```text
putObject(path, localFile)
getObject(path, localFile)
listObjects(prefix)
deleteObject(path)
statObject(path)
```

## 当前结论

百度网盘值得作为第一云后端候选，但不应被理解为 NAS。

它更像一个容量大、用户熟悉、但受平台规则约束的云对象存储后端。当前阶段已经不必为了“用户是否要创建应用”继续深挖；等正式进入云后端开发时，再验证 OAuth、上传、下载、列表、hash 校验、速度、限流和审核即可。

云同步传输格式以 `savelink-cloud-sync-model-protocol-draft.md` 为准：不把整个 `latest.zip` 覆盖上传作为主线，而是验证云端目录结构 + 增量同步。若百度网盘大量小文件体验较差，再考虑单快照 zip。如果这些环节卡住，应及时转向 WebDAV、NAS、本地目录或其他网盘 API。
