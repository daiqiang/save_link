# SaveLink MVP 产品原型草案

## 版本历史

| 版本 | 修改人 | 时间 | 备注 |
| --- | --- | --- | --- |
| 1.0 | 代强 | 2026-06-18 | 第一版：定义 SaveLink 本地存档时间线 MVP 的产品流程、页面草案和安全规则 |
| 1.1 | 代强 | 2026-07-08 | 同步当前实现与云同步路线：FsStore 目录复制、云端增量同步、zip 导出降为低优先级 |
| 1.2 | 代强 | 2026-07-14 | 同步百度网盘 POC 决策：本地目录快照与云端单快照 zip + `.ok` 分层；统一存储术语 |

## 一页摘要

SaveLink 第一阶段要做的是一个面向玩家的 Ludusavi + Kopia 二合一软件。

但它不是简单套壳，而是把两类能力合并成一个玩家能理解的产品：

- 类 Ludusavi 能力：添加游戏、管理存档目录、理解某个游戏有哪些存档文件。
- 类 Kopia 能力：创建快照、保留历史版本、恢复任意历史点、保护当前状态。

MVP 的核心闭环：

```text
添加游戏
→ 选择存档目录
→ 创建快照
→ 查看时间线
→ 选择快照
→ 恢复前自动备份当前存档
→ 一键恢复
```

第一阶段先做本地版，不做云同步、不做百度网盘 API、不做完整自动识别库。只要本地一键回档体验顺，SaveLink 就已经能解决死档、误删、关键节点回退、卸载后找回等核心问题。

## 产品定位

SaveLink 是一个给模拟器、单机游戏和非官方云存档游戏使用的存档时间机器。

第一阶段定位：

> 本地游戏存档时间线工具。

用户不需要理解备份仓库、源目录、目标目录、Kopia 快照、Ludusavi 聚合目录等概念。用户只需要看到：

```text
某个游戏
→ 某个时间点的存档
→ 恢复这个版本
```

## MVP 范围

### 第一版必须做

- 手动添加游戏。
- 为每个游戏配置一个或多个存档目录。
- 手动创建快照。
- 快照列表按时间线展示。
- 快照支持备注。
- 快照支持锁定，锁定后不会被自动清理。
- 一键恢复某个快照。
- 恢复前自动备份当前存档。
- 恢复后保留恢复日志。
- 本地 SQLite 或等价结构保存游戏、目录、快照元数据。
- 本地快照仓库存储实际文件。

### 第一版暂不做

- 百度网盘、阿里云盘、夸克网盘 API。
- 跨设备同步。
- 官方账号系统。
- 自动识别全部游戏。
- 自动识别全部模拟器。
- 游戏启动器。
- 游戏进程检测。
- 游戏退出后自动快照。
- 增量去重和复杂加密。
- 多设备冲突合并。

这些能力不是不重要，而是应该在本地闭环跑顺后再做。

## 核心用户流程

### 流程 1：第一次使用

```text
打开 SaveLink
→ 页面为空
→ 点击“添加游戏”
→ 输入游戏名称
→ 选择存档目录
→ 选择快照仓库位置或使用默认位置
→ 保存
→ 进入游戏详情页
→ 点击“创建第一个快照”
```

关键点：

- 第一次使用不要求登录。
- 不要求配置云盘。
- 不要求用户理解仓库结构。
- 默认快照仓库应由 SaveLink 自动创建。

### 流程 2：手动创建快照

```text
进入游戏详情页
→ 点击“创建快照”
→ SaveLink 扫描存档目录
→ 计算文件数量、总大小、hash
→ 如果与上次快照完全一致，提示“存档未变化”
→ 如果有变化，创建快照
→ 用户可填写备注
→ 快照出现在时间线顶部
```

备注示例：

- Boss 前
- 打完 Boss
- 结局前
- DLC 开始前
- 误操作前备份

### 流程 3：恢复快照

```text
进入游戏详情页
→ 在时间线中选择一个快照
→ 点击“恢复”
→ SaveLink 显示确认弹窗
→ SaveLink 自动备份当前真实存档
→ 清理或覆盖真实存档目录
→ 将目标快照恢复到真实存档目录
→ 记录恢复日志
→ 提示恢复完成
```

恢复确认弹窗必须说清楚：

- 将恢复哪个游戏。
- 将恢复到哪个时间点。
- 当前存档会先自动备份。
- 该操作会覆盖当前真实存档目录中的对应文件。

### 流程 4：恢复后后悔

```text
用户恢复了旧快照
→ 发现恢复错了
→ 在时间线里看到“恢复前自动备份”
→ 点击恢复该自动备份
→ 回到恢复前状态
```

这是 MVP 的生命线。任何恢复动作都必须有回退点。

## 页面草案

### 1. 主界面

主界面用于管理所有游戏。

布局建议：

```text
左侧：游戏列表
右侧：当前选中游戏详情
顶部：添加游戏、设置
```

游戏列表卡片信息：

- 游戏名称。
- 存档目录数量。
- 最近快照时间。
- 快照数量。
- 状态：正常 / 存档目录不存在 / 快照失败 / 需要检查。

空状态：

```text
还没有添加游戏
[添加游戏]
```

不要在空状态堆太多说明文字。第一步只引导用户添加游戏。

### 2. 添加游戏弹窗

字段：

- 游戏名称，必填。
- 存档目录，必填，可添加多个。
- 游戏图标，可选。
- 快照仓库位置，默认。

操作：

- 添加目录。
- 删除目录。
- 测试读取。
- 保存。

校验：

- 游戏名称不能为空。
- 至少选择一个存在的目录。
- 如果目录为空，允许保存，但提示“当前目录为空，创建快照时可能没有文件”。
- 如果目录无法访问，禁止保存或要求用户确认。

### 3. 游戏详情页

详情页是 MVP 最核心页面。

区域：

- 游戏标题。
- 存档目录列表。
- 快照统计。
- 主操作按钮：创建快照。
- 时间线列表。

建议信息结构：

```text
艾尔登法环
存档目录：C:\Users\...\AppData\Roaming\EldenRing\...
快照：12 个
最近快照：2026-06-18 21:30

[创建快照]

时间线
2026-06-18 21:30  Boss 前             [恢复] [锁定] [更多]
2026-06-18 20:10  打完支线             [恢复] [锁定] [更多]
2026-06-17 23:40  恢复前自动备份        [恢复] [锁定] [更多]
```

### 4. 快照详情抽屉

点击某个快照后，可以打开详情。

展示：

- 快照时间。
- 备注。
- 文件数量。
- 总大小。
- 存档目录。
- 文件变更摘要，如果能计算。
- 是否锁定。
- 创建原因：手动 / 恢复前自动备份 / 后续自动快照。

操作：

- 恢复。
- 修改备注。
- 锁定/取消锁定。
- 删除。
- 打开快照位置，高级功能，可后置。

### 5. 恢复确认弹窗

弹窗标题：

```text
恢复到这个存档版本？
```

内容：

```text
游戏：艾尔登法环
目标版本：2026-06-18 21:30 Boss 前

SaveLink 会先自动备份当前存档，然后把该版本恢复到游戏存档目录。
```

按钮：

- 取消。
- 恢复。

恢复按钮可以在二次确认后才可点击，避免误触。

## 数据概念

### 游戏

游戏是用户管理存档的基本单位。

字段建议：

```text
game_id
name
icon
save_paths[]
created_at
updated_at
```

### 存档目录

一个游戏可以有多个存档目录。

例如：

- 一个目录保存 save file。
- 一个目录保存 save state。
- 一个目录保存配置或截图，后续可选。

字段建议：

```text
path_id
game_id
path
label
include_patterns
exclude_patterns
```

MVP 可以先不做 include/exclude UI，但底层可以预留。

### 快照

快照是某个游戏在某个时间点的存档状态。

字段建议：

```text
snapshot_id
game_id
created_at
note
reason
locked
file_count
total_size
content_hash
storage_path
```

reason 取值：

- manual：手动创建。
- before_restore：恢复前自动备份。
- auto：后续自动创建。

### 快照仓库

快照仓库保存实际存档文件。

早期原型曾假设 MVP 可以先使用 zip 包：

```text
repository/
  games/
    {game_id}/
      snapshots/
        {snapshot_id}.zip
      metadata/
        {snapshot_id}.json
  savelink.db
```

早期 zip 方案假设的好处：

- 实现简单。
- 方便手动上传到云端。
- 方便迁移和导出。
- 用户能理解“一个快照包”。

缺点：

- 大量重复文件会占空间。
- 没有 Kopia 那种成熟去重。

MVP 可以接受这个缺点。后续再考虑内容寻址存储、增量或接入 Kopia。

当前实现修正：

- MVP 已由 `FsStore` 将快照以目录形式保存在 `repository/snapshots/{snapshot_id}/`，并在同级写入本机 `{snapshot_id}.ok` 完整性标记。
- 整个 `latest.zip` 不适合作为自动云同步主线，因为新增一个快照也可能重传整个历史包。
- 云同步采用云端目录结构 + 增量同步；百度网盘已确定“单快照 zip + `.ok`”，而不是整个 SaveLink 数据区打成一个 zip。
- 手动 zip 导出/导入仍可作为备份迁移工具，但当前优先级低。

## 安全规则

### 规则 1：永远不直接同步真实存档目录

真实存档目录只作为读取和恢复目标。

跨设备或云端同步时，不同步真实存档目录。云端应同步 SaveLink 的共享快照事实和云端元数据；本机真实存档路径只作为本机绑定保存，不能被另一台电脑的路径覆盖。

### 规则 2：恢复前必须自动备份当前状态

任何恢复动作都必须先创建一个 `before_restore` 快照。

如果自动备份失败，恢复必须中止。

### 规则 3：快照默认不可变

快照一旦创建，内容不应被修改。

允许修改：

- 备注。
- 锁定状态。

不允许修改：

- 快照文件内容。
- 创建时间。
- 文件 hash。

### 规则 4：删除快照要谨慎

删除快照至少需要确认。

锁定快照不能直接删除，必须先取消锁定。

### 规则 5：路径不存在时不自动创建恢复

如果真实存档目录不存在，恢复前需要提示用户。

允许用户选择：

- 创建目录并恢复。
- 重新选择存档目录。
- 取消。

## 与 Ludusavi + Kopia 的关系

SaveLink 第一阶段的产品形态可以理解为 Ludusavi + Kopia 二合一。

但实现上不必一开始就依赖 Ludusavi 和 Kopia。

第一阶段自研最小内核：

- 手动添加游戏。
- 手动配置存档目录。
- zip 快照。
- SQLite 元数据。
- 一键恢复。

后续可以吸收 Ludusavi 的能力：

- 游戏识别数据库。
- 常见游戏存档路径。
- 平台识别，例如 Steam、GOG、Epic。

后续可以吸收 Kopia 的能力：

- 去重。
- 压缩。
- 加密。
- 增量。
- 更成熟的仓库结构。

但产品 UI 不应该暴露 Ludusavi 或 Kopia 的原始概念。

## 后续路线

### 阶段 1：本地 MVP

目标：做出本地一键回档闭环。

范围：

- 手动添加游戏。
- 创建快照。
- 时间线。
- 恢复。
- 恢复前自动备份。

### 阶段 2：自动化

目标：减少用户手动操作。

范围：

- 文件变化检测。
- 游戏启动/退出检测。
- 游戏退出后自动快照。
- 如果文件未变化，不创建重复快照。
- 保留策略。

### 阶段 3：游戏识别

目标：降低添加游戏成本。

范围：

- 引入或参考 Ludusavi 数据。
- 常见游戏路径预设。
- 常见模拟器路径预设。

### 阶段 4：云端存储

目标：跨设备使用。

范围：

- 百度网盘 API。
- WebDAV。
- NAS/SMB。
- 本地目录同步。
- 其他网盘 API。

## 当前结论

下一步不需要先画高保真原型图。

更合适的顺序是：

```text
先确认本文档的产品流程
→ 再画低保真线框图
→ 再定技术架构
→ 再做本地 MVP
```

SaveLink 第一阶段的胜负手不是云同步，而是：

> 能不能把“创建存档快照”和“一键回档”做得足够简单、可靠、放心。
